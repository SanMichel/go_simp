---
phase: 06-testing-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - cmd/server/main_test.go
autonomous: true
requirements:
  - TEST-02
  - TEST-05
user_setup: []
must_haves:
  truths:
    - "CSRF middleware rejects POST/PATCH/DELETE on /api/ without cookie+header token"
    - "CSRF middleware skips /login and /api/auth/login POST endpoints"
    - "CSRF middleware checks Origin header for non-login POSTs"
    - "CSRF middleware rejects PATCH/DELETE methods without CSRF token"
    - "securityHeaders omits HSTS in non-production mode"
    - "mapUser, mapActivity, mapProduct, mapOracleProduct produce correct API structs"
    - "mapProduct handles all SQL null fields (RuaLida, PredioLido, DescCompleta, etc.)"
    - "mapOracleProduct handles all SQL null fields"
    - "parseFilters extracts query params into ActivityFilters correctly"
    - "intQuery converts query param to int, defaults to 0 on error"
    - "firstNonEmpty returns first non-empty string"
    - "rateLimiter.allow blocks after 5 attempts per IP, reaps stale entries"
    - "loadConfig sets defaults for all optional env vars"
    - "healthCheck returns 200 with status ok"
    - "home handler renders home template"
    - "loginPage redirects to role-based home when already authenticated"
    - "style, adminStyle serve embedded CSS files"
    - "serveJS serves embedded JS files"
  artifacts:
    - path: cmd/server/main_test.go
      provides: "Auth middleware, mapping, utility, non-DB handler tests"
      min_lines: 900
  key_links:
    - from: "main_test.go"
      to: "auth.go"
      via: "tests csrfMiddleware, securityHeaders, setCSRFCookie, clearCSRFCookie"
    - from: "main_test.go"
      to: "api_handlers.go"
      via: "tests mapUser, mapActivity, mapProduct, mapOracleProduct"
    - from: "main_test.go"
      to: "utils.go"
      via: "tests parseFilters, intQuery, firstNonEmpty, rateLimiter, loadConfig"
    - from: "main_test.go"
      to: "handlers.go"
      via: "tests healthCheck, home, loginPage via httptest"
    - from: "main_test.go"
      to: "main.go"
      via: "tests style, adminStyle, serveJS via httptest"
---

<objective>
**Phase 6, Plan 02: Expanded Unit Tests — Auth Middleware + Mapping Functions + Utilities + Non-DB Handlers**

Add comprehensive tests for CSRF middleware, security headers, mapping functions, utility functions, rate limiter, loadConfig, and all non-DB-dependent handlers. These tests exercise no database and always run.

**Purpose:** Push coverage to ~35% by covering all code paths that don't require Postgres. Validate CSRF middleware behavior (origin check, skip patterns, method coverage), mapping function correctness (including SQL NULL handling), utility function edge cases, and static file handlers.

**Output:** Expanded `cmd/server/main_test.go` with ~18 additional test functions.
</objective>

<execution_context>
@/home/bblosangeles/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/bblosangeles/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-testing-infrastructure/06-RESEARCH.md
@cmd/server/auth.go
@cmd/server/api_handlers.go
@cmd/server/utils.go
@cmd/server/handlers.go
@cmd/server/main.go
@cmd/server/main_test.go
@cmd/server/models.go
@cmd/server/dashboard_handlers.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSRF middleware expanded tests and security headers edge case tests</name>
  <files>cmd/server/main_test.go</files>
  <read_first>
    cmd/server/auth.go — csrfMiddleware, setCSRFCookie, clearCSRFCookie, securityHeaders lines 156-184, 262-273
    cmd/server/main_test.go — existing TestCSRFMiddleware* tests (lines 258-299) for pattern reference
    cmd/server/errors.go — handleError signature, AppError ErrCodeForbidden
    .planning/phases/06-testing-infrastructure/06-RESEARCH.md — Patterns 5 (handleError tests) for CSRF error path assertions
  </read_first>
  <action>
    Append to `cmd/server/main_test.go` after the last test function with a section comment `// ---- CSRF Middleware Tests ----`.

    Add these tests (each creates `&App{cfg: Config{SessionSecret: []byte("test-secret-32-chars-minimum-length!")}}` directly — NO testApp needed):

    1. `TestCSRFMiddlewareAllowsGET(t *testing.T)`:
       - GET /api/test with no CSRF token — should pass (GET is not protected)
       - Assert `rec.Code == 200`

    2. `TestCSRFMiddlewareSkipsLoginPost(t *testing.T)`:
       - POST /login with no CSRF token — should pass per skipCSRF logic
       - Assert `rec.Code == 200`

    3. `TestCSRFMiddlewareSkipsAPILoginPost(t *testing.T)`:
       - POST /api/auth/login with no CSRF token — should pass per skipCSRF logic
       - Assert `rec.Code == 200`

    4. `TestCSRFMiddlewareBlocksAPIWithoutCookie(t *testing.T)`:
       - POST /api with X-CSRF-Token header but no csrf_token cookie — should be 403
       - Assert `rec.Code == 403`, body indicates missing CSRF token
       - (Builds on existing TestCSRFMiddlewareBlocksAPIWithoutToken which uses neither)

    5. `TestCSRFMiddlewareBlocksAPIWithoutHeader(t *testing.T)`:
       - POST /api with cookie but no X-CSRF-Token header — should be 403
       - Assert `rec.Code == 403`

    6. `TestCSRFMiddlewareBlocksOnOriginMismatch(t *testing.T)`:
       - POST /some-path with `Origin: https://evil.com` header — should be 403
       - Assert `rec.Code == 403`

    7. `TestCSRFMiddlewareAllowsOriginMatch(t *testing.T)`:
       - POST /some-path with `Origin: https://example.com` where `r.Host == "example.com"` — should pass
       - Build request with `r.Host = "example.com"`, `Origin: "https://example.com"`
       - Assert `rec.Code == 200`

    8. `TestCSRFMiddlewareBlocksPATCH(t *testing.T)`:
       - PATCH /api without CSRF — should be 403
       - Assert `rec.Code == 403`

    9. `TestCSRFMiddlewareBlocksDELETE(t *testing.T)`:
       - DELETE /api without CSRF — should be 403
       - Assert `rec.Code == 403`

    Then add `// ---- Security Headers Tests ----`:

    10. `TestSecurityHeadersDevModeNoHSTS(t *testing.T)`:
        - `App{cfg: Config{AppEnv: "development"}}`
        - Assert HSTS header is empty
        - Assert X-Content-Type-Options, X-Frame-Options, Referrer-Policy, CSP are set

    DO NOT modify existing test functions. Each test constructs its own minimal App.
    These tests do NOT need DB — they use inner next handlers that write 200 directly.
  </action>
  <verify>
    <automated>go test -run 'TestCSRF|TestSecurity' ./cmd/server -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - 10 test functions added (9 CSRF + 1 security headers)
    - All pass without external dependencies
    - Origin mismatch returns 403
    - Login POST skips CSRF check
    - PATCH/DELETE methods are protected
    - HSTS absent in development mode
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add mapping function and utility function tests</name>
  <files>cmd/server/main_test.go</files>
  <read_first>
    cmd/server/api_handlers.go — mapActivity, mapProduct, mapUser, mapOracleProduct functions (lines 51-175)
    cmd/server/models.go — Activity, ProductVerification, OracleProduct, UserRow structs
    cmd/server/utils.go — parseFilters, intQuery, firstNonEmpty, rateLimiter, loadConfig, getenv
    cmd/server/main_test.go — existing TestFirstNonEmpty (line 138), TestRateLimiter (line 184), TestLoadConfigRequiresSecret (line 237) for evolution context
    cmd/server/dashboard_handlers.go — parseFilters usage context
  </read_first>
  <action>
    Append to `cmd/server/main_test.go` with section comments:

    `// ---- Mapping Function Tests ----`:

    1. `TestMapUser(t *testing.T)`:
       - Input: `UserRow{ID: 1, Username: "joao", Role: "gerente"}`
       - Assert `apiUser.ID == 1`, `apiUser.Username == "joao"`, `apiUser.Role == "gerente"`

    2. `TestMapActivity(t *testing.T)`:
       - Input: Activity with known values, DataFim set to time.Now()
       - Assert all fields map correctly, DataFim is non-nil pointer
       - Input with zero DataFim: assert DataFim is nil

    3. `TestMapProduct(t *testing.T)`:
       - Table-driven: (a) all fields valid, (b) all SQL null fields (RuaLida invalid, PredioLido invalid, etc.), (c) DataEntrada valid → formatted as RFC3339
       - Assert nil vs non-nil pointer for each sql.Null* field
       - Assert DataEntrada format is RFC3339 when valid

    4. `TestMapOracleProduct(t *testing.T)`:
       - Table-driven: (a) all fields valid, (b) all SQL null fields
       - Assert nil vs non-nil pointer for optional fields
       - Assert Codigos, DescCompleta, etc. mapped correctly

    `// ---- Utility Function Tests ----`:

    5. `TestParseFilters(t *testing.T)`:
       - Build URLs with query params: `filter_username[]=joao&filter_empresa[]=001&filter_rua[]=RUA1&sort=dataFim&order=asc`
       - Assert ActivityFilters fields match
       - With empty query: assert all slices are nil/empty

    6. `TestIntQuery(t *testing.T)`:
       - `?x=42` → 42, `?x=abc` → 0, no param → 0, `?x=-1` → 0 (Atoi returns 0 on -1? No, Atoi("-1") = -1)
       - Actually test: `?x=42` → 42, `?x=abc` → 0, missing → 0

    7. `TestFirstNonEmptyEdgeCases(t *testing.T)`:
       - Both empty → returns empty
       - First empty, second non-empty → returns second
       - First non-empty, second non-empty → returns first

    8. `TestRateLimiterReapStaleEntries(t *testing.T)`:
       - Allow 5 requests from same IP
       - Verify different IP is allowed
       - NOTE: For goroutine-based cleanup, just verify the allow logic; the goroutine runs on a 1min timer which is impractical to test

    `// ---- Config Loading Tests ----`:

    9. `TestLoadConfigDefaults(t *testing.T)`:
        - Set env vars: `SESSION_SECRET=this-is-a-32-char-secret-for-testing!`, `POSTGRES_URL=postgres://localhost:5432/test`
        - Assert `cfg.Port == "3000"`, `cfg.AppEnv == "development"`, `cfg.SessionTTL == 8h`
        - Assert `cfg.PGMaxConns == 10`
        - Clear env vars in defer

    10. `TestLoadConfigCustomEnv(t *testing.T)`:
        - Set PORT=8080, APP_ENV=production, SESSION_TTL=1h, PG_MAX_CONNS=20
        - Assert custom values take effect
        - Clear env vars in defer

    DO NOT use `t.Setenv` (requires Go 1.22+). Use `os.Setenv` + `defer os.Unsetenv`.
    Use `os.Clearenv()` is NOT safe — it clears all env vars. Instead, save and restore specific keys.
  </action>
  <verify>
    <automated>go test -run 'TestMapUser|TestMapActivity|TestMapProduct|TestMapOracleProduct|TestParseFilters|TestIntQuery|TestFirstNonEmpty|TestRateLimiter|TestLoadConfig' ./cmd/server -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - 10 test functions added (4 mapping + 6 utility/config)
    - All pass without external dependencies
    - MapProduct handles all sql.Null* field states (valid/invalid)
    - Mapping functions produce correct pointer vs non-pointer for optional fields
    - loadConfig defaults are correct (Port=3000, AppEnv=development, SessionTTL=8h)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add non-DB handler tests for healthCheck, home, loginPage, style, adminStyle, serveJS, and firstNonEmpty</name>
  <files>cmd/server/main_test.go</files>
  <read_first>
    cmd/server/handlers.go — healthCheck, home, loginPage functions
    cmd/server/main.go — style, adminStyle, serveJS functions
    cmd/server/main_test.go — existing TestHealthCheckResponse (line 312) for pattern reference
  </read_first>
  <action>
    Append to `cmd/server/main_test.go` with section comment `// ---- Non-DB Handler Tests ----`:

    These tests use `httptest.NewRecorder` + `httptest.NewRequest` with minimal App construction (NO testApp, no DB).

    1. Replace existing `TestHealthCheckResponse` with an expanded table-driven `TestHealthCheckHandler`:
       - Case: "returns 200 with status ok" — GET /api/health, `&App{loginLimiter: newRateLimiter()}` (ora=nil)
       - Assert rec.Code == 200, Content-Type == application/json; charset=utf-8
       - JSON-unmarshal body: assert `body["status"] == "ok"`
       - (Keep the existing test name for backward compatibility or rename)

    2. `TestHomeHandler(t *testing.T)`:
       - `&App{tpl: parseTemplates()}`, GET /home
       - Assert rec.Code == 200, Content-Type starts with text/html

    3. `TestLoginPageUnauthenticated(t *testing.T)`:
       - `&App{tpl: parseTemplates()}`, GET /login (no cookie)
       - Assert rec.Code == 200, renders login template

    4. `TestStyleHandler(t *testing.T)`:
       - App{}, GET /style.css
       - Assert rec.Code == 200, Content-Type == text/css; charset=utf-8
       - (This requires `templatesFS` which is package-level; direct call to `app.style(rec, req)`)

    5. `TestAdminStyleHandler(t *testing.T)`:
       - Same pattern as TestStyleHandler for /admin.css

    6. `TestServeJSHandler(t *testing.T)`:
       - Test shared.js: GET /shared.js, assert 200 + application/javascript Content-Type
       - Test htmx.min.js: GET /htmx.min.js, assert 200

    (Tasks 1-2 in this plan may have already covered TestFirstNonEmpty etc., so no need to duplicate.)

    DO NOT modify existing tests except TestHealthCheckResponse which can be expanded or replaced.
    Use `t.Run` sub-tests where table-driven style is natural (e.g., serveJS with multiple paths).
  </action>
  <verify>
    <automated>go test -run 'TestHealthCheck|TestHome|TestLoginPage|TestStyle|TestAdminStyle|TestServeJS' ./cmd/server -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - 5-6 test functions covering non-DB handlers
    - healthCheck returns `{"status":"ok"}` with JSON content type
    - home returns HTML 200
    - loginPage returns HTML 200 when unauthenticated
    - Static file handlers return correct content types
    - `go test -count=1 ./cmd/server` passes with no external deps
  </acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase Produces

### Functions/Symbols Created (Test Functions)

| Symbol | File | Coverage Target |
|--------|------|-----------------|
| `TestCSRFMiddlewareAllowsGET` | main_test.go | csrfMiddleware GET pass-through |
| `TestCSRFMiddlewareSkipsLoginPost` | main_test.go | /login skip |
| `TestCSRFMiddlewareSkipsAPILoginPost` | main_test.go | /api/auth/login skip |
| `TestCSRFMiddlewareBlocksAPIWithoutCookie` | main_test.go | cookie-missing 403 |
| `TestCSRFMiddlewareBlocksAPIWithoutHeader` | main_test.go | header-missing 403 |
| `TestCSRFMiddlewareBlocksOnOriginMismatch` | main_test.go | origin check 403 |
| `TestCSRFMiddlewareAllowsOriginMatch` | main_test.go | origin match pass |
| `TestCSRFMiddlewareBlocksPATCH` | main_test.go | PATCH protection |
| `TestCSRFMiddlewareBlocksDELETE` | main_test.go | DELETE protection |
| `TestSecurityHeadersDevModeNoHSTS` | main_test.go | HSTS omitted in dev |
| `TestMapUser` | main_test.go | mapUser |
| `TestMapActivity` | main_test.go | mapActivity |
| `TestMapProduct` | main_test.go | mapProduct |
| `TestMapOracleProduct` | main_test.go | mapOracleProduct |
| `TestParseFilters` | main_test.go | parseFilters |
| `TestIntQuery` | main_test.go | intQuery |
| `TestFirstNonEmptyEdgeCases` | main_test.go | firstNonEmpty |
| `TestRateLimiterReapStaleEntries` | main_test.go | rateLimiter |
| `TestLoadConfigDefaults` | main_test.go | loadConfig defaults |
| `TestLoadConfigCustomEnv` | main_test.go | loadConfig overrides |
| `TestHealthCheckHandler` | main_test.go | healthCheck |
| `TestHomeHandler` | main_test.go | home handler |
| `TestLoginPageUnauthenticated` | main_test.go | loginPage |
| `TestStyleHandler` | main_test.go | style handler |
| `TestAdminStyleHandler` | main_test.go | adminStyle |
| `TestServeJSHandler` | main_test.go | serveJS |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSRF middleware | Origin header validation, cookie+header token matching |
| Config loading | SESSION_SECRET length requirement enforced at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Spoofing | CSRF middleware | mitigate | TestCSRFMiddlewareBlocksOnOriginMismatch asserts Origin mismatch returns 403. TestCSRFMiddlewareAllowsOriginMatch asserts valid Origin passes. Cookie+token dual validation tested |
| T-06-07 | Information Disclosure | Config logging | mitigate | TestLoadConfigDefaults/Tests verify loadConfig reads from env only. No config values are logged by the function itself. SESSION_SECRET is never printed |
| T-06-08 | Repudiation | Map function null handling | accept | mapProduct/mapOracleProduct tests verify sql.Null* field → nil pointer behavior. If a new nullable field is added without test, it may crash on nil. Mitigated by coverage requirement |
| T-06-09 | Information Disclosure | healthCheck oracle_down status | accept | The health check exposes Oracle connectivity status. This is a standard health endpoint pattern, no PII exposed |
| T-06-SC | Tampering | No new packages | mitigate | All tests use Go standard library only. `go mod verify` confirms no tampered dependencies |
</threat_model>

<verification>
## Plan-Level Verification

| Check | Command |
|-------|---------|
| All tests in this plan pass | `go test -run 'TestCSRF\|TestSecurity\|TestMap\|TestParseFilters\|TestIntQuery\|TestFirstNonEmpty\|TestRateLimiter\|TestLoadConfig\|TestHealthCheck\|TestHome\|TestLoginPage\|TestStyle\|TestAdminStyle\|TestServeJS' ./cmd/server -v -count=1` |
| All existing tests still pass | `go test ./cmd/server -count=1` |
| Build succeeds | `go build ./cmd/server` |
</verification>

<success_criteria>
- [ ] 26 new test functions in main_test.go (10 CSRF/security + 4 mapping + 6 utility + 6 non-DB handler)
- [ ] All pass with `go test -count=1 ./cmd/server` (no DB needed)
- [ ] Coverage increases to ~35-40%
- [ ] Origin-based CSRF protection fully tested
- [ ] All mapping functions tested with SQL NULL and valid states
- [ ] loadConfig defaults and overrides verified
</success_criteria>

<output>
Create `.planning/phases/06-testing-infrastructure/06-02-SUMMARY.md` when done
</output>
