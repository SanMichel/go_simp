---
phase: 06-testing-infrastructure
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - cmd/server/main_test.go
  - .env.example
autonomous: true
requirements:
  - TEST-01
  - TEST-03
  - TEST-05
  - TEST-06
user_setup:
  - service: Postgres (test database)
    why: "Handler and route integration tests require a real Postgres connection"
    env_vars:
      - name: TEST_POSTGRES_URL
        value: "postgres://user:password@localhost:5432/go_simp_test?sslmode=disable"
        source: "Your local Postgres instance or docker-compose"
    notes: "Document in .env.example. Without TEST_POSTGRES_URL, all DB-dependent tests in this plan skip gracefully."
must_haves:
  truths:
    - "loginPost with valid credentials sets token cookie and redirects"
    - "loginPost with invalid credentials renders login page with error"
    - "loginPost rate-limited after 5 attempts returns login page with rate-limit error"
    - "logout clears token and csrf cookies, redirects to /login"
    - "apiLogin with valid credentials returns 200 with user JSON"
    - "apiLogin with invalid credentials returns 401"
    - "apiLogin rate-limited after 5 attempts returns 429"
    - "apiLogout clears cookies and returns 200"
    - "apiMe with valid token returns user JSON"
    - "apiMe without token returns 401"
    - "atividadesPage renders with User context when authenticated"
    - "dashboardPage renders with Activities, Options, Filters when authenticated"
    - "dashboardTable returns activities_table partial when authenticated"
    - "adminPage renders with Users list when authenticated"
    - "adminCreateUser creates user and renders users_section"
    - "adminUpdateUser updates user role and renders user_row"
    - "adminEditUserRow renders edit row partial"
    - "adminUserRow renders user row partial"
    - "apiLastInfo returns null JSON for unknown activity"
    - "listActivities returns activities ordered by data_fim DESC"
    - "listActivities respects limit parameter"
    - "listFilterOptions returns distinct filter values"
    - "activityDetailsData returns Activity + Products for valid ID"
    - "activityDetailsData returns error for invalid ID"
    - "POST /api/atividades/finalizar creates activity and returns success JSON"
    - "All 46 routes return correct status codes (public and authenticated)"
    - "Coverage is >= 70% across the codebase"
  artifacts:
    - path: cmd/server/main_test.go
      provides: "All handler integration tests, route integration, coverage gate"
      min_lines: 2200
    - path: .env.example
      provides: "Documentation for TEST_POSTGRES_URL"
  key_links:
    - from: "main_test.go"
      to: "testhelper.go"
      via: "calls testApp(), testUser(), testToken()"
    - from: "main_test.go"
      to: "handlers.go"
      via: "tests loginPost, logout, apiMe, apiLogin, apiLogout, atividadesPage via httptest"
    - from: "main_test.go"
      to: "activity_handlers.go"
      via: "tests apiLastInfo, apiFinalizar via httptest"
    - from: "main_test.go"
      to: "dashboard_handlers.go"
      via: "tests dashboardPage, dashboardTable via httptest"
    - from: "main_test.go"
      to: "admin_handlers.go"
      via: "tests adminPage, adminCreateUser, adminUpdateUser via httptest"
    - from: "main_test.go"
      to: "api_handlers.go"
      via: "tests apiAdminUsersList, apiAdminUserCreate, apiAdminUserUpdate, apiDashboardActivities, apiDashboardActivityDetails, apiDashboardBulkDetails, apiDashboardBulkPrint"
    - from: "main_test.go"
      to: "db.go"
      via: "tests listActivities, listFilterOptions, activityDetailsData"
---

<objective>
**Phase 6, Plan 04: DB-dependent Handler Tests + Route Integration + Coverage Gate**

Add database-dependent handler tests covering all auth handlers (loginPost, apiLogin, logout, apiLogout, apiMe), activity handlers (apiLastInfo, apiFinalizar), dashboard handlers, admin handlers, and API handlers. Add a route integration test that exercises the full `routes()` mux. Add DB query tests for listActivities, listFilterOptions, activityDetailsData. Document `TEST_POSTGRES_URL` in `.env.example`. Verify coverage reaches ≥ 70%.

**Purpose:** Achieve ≥70% coverage target by testing all DB-dependent code paths. Validate that every route returns the correct status code and response shape when accessed through the full mux.

**Output:** Final expanded `cmd/server/main_test.go` reaching ~80+ test functions, `.env.example` update, and coverage verification.
</objective>

<execution_context>
@/home/bblosangeles/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/bblosangeles/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-testing-infrastructure/06-RESEARCH.md
@.planning/phases/06-testing-infrastructure/06-VALIDATION.md
@cmd/server/handlers.go
@cmd/server/activity_handlers.go
@cmd/server/dashboard_handlers.go
@cmd/server/admin_handlers.go
@cmd/server/api_handlers.go
@cmd/server/db.go
@cmd/server/main.go
@cmd/server/main_test.go
@cmd/server/models.go
@cmd/server/testhelper.go
@cmd/server/auth.go
@cmd/server/errors.go
@cmd/server/utils.go
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auth handler tests (loginPost, logout, apiMe, apiLogin, apiLogout, atividadesPage)</name>
  <files>cmd/server/main_test.go</files>
  <read_first>
    cmd/server/handlers.go — loginPost (lines 22-44), logout (lines 56-63), apiMe (lines 70-77), apiLogin (lines 79-107), apiLogout (lines 109-116), atividadesPage (lines 65-68)
    cmd/server/testhelper.go — testApp, testUser, testToken function signatures
    cmd/server/models.go — User struct
    cmd/server/errors.go — ErrCodeRateLimited (429), ErrCodeBadRequest (400), ErrCodeUnauthorized (401)
  </read_first>
  <action>
    Append to `cmd/server/main_test.go` with section comment `// ---- Auth Handler Integration Tests ----`.

    All tests call `testApp(t)`. All DB operations use testApp's `a.pg`.

    1. `TestLoginPostSuccess(t *testing.T)`:
       - Seed user: `testUser(t, app, "test_login_success", "gerente", "pass1234")`
       - `httptest.NewRequest("POST", "/login", urlencoded form "username=test_login_success&password=pass1234")`
       - Set Content-Type to `application/x-www-form-urlencoded`
       - Call `app.loginPost(rec, req)`
       - Assert rec.Code == 302 (Found — redirectByRole)
       - Assert `Set-Cookie` header contains `token=`
       - Assert `Set-Cookie` header contains `csrf_token=`

    2. `TestLoginPostWrongPassword(t *testing.T)`:
       - Seed user with password "pass1234"
       - POST with password "wrongpass"
       - Assert rec.Code == 200 (login page re-rendered with error)
       - Assert response body contains "incorretos" or error message

    3. `TestLoginPostRateLimited(t *testing.T)`:
       - Use a unique remote address for rate limiter isolation
       - Send 5 rapid requests with wrong password (using same app to share rateLimiter)
       - 6th request: Assert rec.Code == 200, body contains rate-limit error message
       - (Rate limiter is per-IP, per-minute; 5 failed attempts then block)

    4. `TestLogoutHandler(t *testing.T)`:
       - Seed user, create token
       - Request with token cookie
       - Assert rec.Code == 302 (redirect to /login)
       - Assert Set-Cookie clears token (MaxAge=-1 or empty value)

    5. `TestAPILoginSuccess(t *testing.T)`:
       - Seed user: `testUser(t, app, "test_apilogin", "sysadmin", "pass1234")`
       - `httptest.NewRequest("POST", "/api/auth/login", json body {"username":"test_apilogin","password":"pass1234"})`
       - Assert rec.Code == 200
       - JSON-unmarshal body, assert `body["user"]["username"] == "test_apilogin"`
       - Assert Set-Cookie contains token= in response

    6. `TestAPILoginInvalidCredentials(t *testing.T)`:
       - POST with wrong password
       - Assert rec.Code == 401
       - JSON-unmarshal body: assert `body["error"] == "Credenciais inválidas"`

    7. `TestAPILoginRateLimited(t *testing.T)`:
       - 6 rapid requests with unique remote address
       - 6th: assert rec.Code == 429 (TooManyRequests)

    8. `TestAPILogout(t *testing.T)`:
       - Seed user, create token, request with cookie
       - Assert rec.Code == 200
       - JSON-unmarshal: assert `body["message"]` contains "Logout"

    9. `TestAPIMeAuthenticated(t *testing.T)`:
       - Seed user, create token
       - Request with token cookie
       - Assert rec.Code == 200, body["user"]["username"] == seeded username

    10. `TestAtividadesPageAuthenticated(t *testing.T)`:
        - Seed conferente user
        - Create token, add to cookie
        - `app.atividadesPage(rec, req)` directly (not through middleware chain)
        - The handler reads `r.Context().Value(ctxUser).(*User)` — need to set user in context
        - Build request, then add user to context: `ctx := context.WithValue(r.Context(), ctxUser, user)`
        - Assert rec.Code == 200, Content-Type text/html

    IMPORTANT: The atividadesPage handler reads user from context (set by requireRole middleware).
    You must set ctxUser in the request context before calling the handler directly.
    Use `context.WithValue(r.Context(), ctxUser, user)` where ctxUser is the unexported context key from auth.go.
    Since it's the same package (package main), ctxUser is accessible.

    For rate limit tests, use unique RemoteAddr each time (e.g., `192.168.1.X` with incrementing X).
  </action>
  <verify>
    <automated>go test -run 'TestLoginPost|TestLogoutHandler|TestAPILogin|TestAPILogout|TestAPIMe|TestAtividadesPage' ./cmd/server -v -count=1 2>&1 || echo "SKIPPED: set TEST_POSTGRES_URL to run"</automated>
  </verify>
  <acceptance_criteria>
    - 10 test functions added for auth handlers
    - loginPost sets token+crsf cookies on success, shows error on failure, rate-limits after 5
    - apiLogin returns 200 with user JSON on success, 401 on bad creds, 429 on rate limit
    - apiMe returns user JSON when authenticated
    - atividadesPage renders HTML when user is in context
    - All skip gracefully when TEST_POSTGRES_URL is unset
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add DB-query tests (listActivities, listFilterOptions, activityDetailsData) and activity handler tests (apiLastInfo, apiFinalizar)</name>
  <files>cmd/server/main_test.go</files>
  <read_first>
    cmd/server/db.go — listActivities (lines 210-298), listFilterOptions (lines 300-324), activityDetailsData (lines 325-370)
    cmd/server/activity_handlers.go — apiLastInfo (lines 130-145), apiFinalizar (lines 147-247)
    cmd/server/models.go — ActivityFilters, FilterOptions, Activity, ProductVerification, finalizeReq structs
  </read_first>
  <action>
    Append to `cmd/server/main_test.go` with section comments:

    `// ---- DB Query Integration Tests ----`:

    1. `TestListActivities(t *testing.T)`:
       - `app := testApp(t)`
       - Assert initial count is 0 (no activities)
       - Seed an activity by directly inserting into atividades + atividade_enderecos tables:
         `INSERT INTO atividades (empresa, seqlocal, usuario_id, data_fim) VALUES ('001', 1, <user_id>, now()) RETURNING id`
         `INSERT INTO atividade_enderecos (atividade_id, rua, predio) VALUES (<id>, 'RUA A', 'PREDIO 1')`
       - `activities, err := app.listActivities(ctx, ActivityFilters{}, 50)`
       - Assert err == nil, len(activities) >= 1
       - Assert first activity has Empresa="001", Rua="RUA A", Predio="PREDIO 1"
       - Test with limit: should return only the specified number
       - Test with no results filter: `filters := ActivityFilters{Empresa: []string{"NONEXISTENT"}}`
         results should be empty

    2. `TestListFilterOptions(t *testing.T)`:
       - Seed an activity with known empresa/rua/predio
       - `options, err := app.listFilterOptions(ctx)`
       - Assert err == nil
       - Assert Empresa contains "001"
       - Assert Rua contains "RUA A"
       - Assert Impresso is ["S", "N"]

    3. `TestActivityDetailsData(t *testing.T)`:
       - Seed an activity with produto_verificacao records
       - `act, items, err := app.activityDetailsData(ctx, activityID)`
       - Assert err == nil
       - Assert act.ID == activityID
       - Assert len(items) >= 1
       - Test with invalid ID: `_, _, err := app.activityDetailsData(ctx, 999999)` — assert err != nil

    `// ---- Activity Handler Tests ----`:

    4. `TestAPILastInfoNoData(t *testing.T)`:
       - `app := testApp(t)`
       - Request GET /api/atividades/last-info?empresa=001&seqlocal=1&rua=RUA A&predio=PREDIO%201
       - Call `app.apiLastInfo(rec, req, &User{})` — needs *User for requireAPIRole wrapper
       - Assert rec.Code == 200 (returns null/nil when no data)
       - Body should be "null" (writeJSON(w, 200, nil))

    5. `TestAPILastInfoWithData(t *testing.T)`:
       - Seed an activity with empresa, seqlocal, rua, predio
       - Request with matching empresa/seqlocal/rua/predio
       - Assert rec.Code == 200
       - JSON-unmarshal body: assert `body["dataFim"]` exists and is a valid time string

    6. `TestAPIFinalizarSuccess(t *testing.T)`:
       - `app := testApp(t)`, seed user
       - Create valid finalizeReq JSON body:
         `{"empresa": 1, "seqlocal": 1, "rua": "RUA A", "predio": ["PREDIO 1"], "readProducts": [], "expectedProducts": []}`
       - Request POST with Content-Type application/json
       - Assert rec.Code == 200
       - JSON-unmarshal: assert `body["success"] == true`, `body["atividadeId"]` is a positive number

    7. `TestAPIFinalizarMissingFields(t *testing.T)`:
       - Missing empresa field: `{"seqlocal": 1, "rua": "RUA A"}`
       - Assert rec.Code == 400 (bad request)

    8. `TestAPIFinalizarWithReadProducts(t *testing.T)`:
       - Valid request with readProducts containing a product
       - Assert rec.Code == 200
       - Assert divergences/ruptures/replenishments arrays exist in response

    For activity seeding, construct INSERT statements directly:
    ```go
    var actID int
    err := app.pg.QueryRowContext(ctx,
      `INSERT INTO atividades (empresa, seqlocal, usuario_id, data_fim) VALUES ($1,$2,$3,now()) RETURNING id`,
      "001", 1, user.ID).Scan(&actID)
    ```
    Use `context.Background()` for all contexts.
    Use unique test user names to avoid collisions with other tests.
  </action>
  <verify>
    <automated>go test -run 'TestListActivities|TestListFilterOptions|TestActivityDetailsData|TestAPILastInfo|TestAPIFinalizar' ./cmd/server -v -count=1 2>&1 || echo "SKIPPED: set TEST_POSTGRES_URL to run"</automated>
  </verify>
  <acceptance_criteria>
    - 8 test functions added (3 DB query + 5 activity handler)
    - listActivities returns correct data, respects limit, returns empty for no-match filters
    - listFilterOptions returns distinct filter values from seeded data
    - activityDetailsData returns activity + items for valid ID, error for invalid
    - apiLastInfo returns null for unknown params, dataFim for known activity
    - apiFinalizar creates activity with success response
    - apiFinalizar rejects requests with missing required fields (400)
    - All skip gracefully when TEST_POSTGRES_URL is unset
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add dashboard/admin handler tests, route integration test, coverage gate, and env.example update</name>
  <files>cmd/server/main_test.go, .env.example</files>
  <read_first>
    cmd/server/admin_handlers.go — adminPage, adminCreateUser, adminUpdateUser, adminEditUserRow, adminUserRow
    cmd/server/dashboard_handlers.go — dashboardPage, dashboardTable, activityDetails (page)
    cmd/server/api_handlers.go — apiAdminUsersList, apiAdminUserCreate, apiAdminUserUpdate, apiDashboardActivities, apiDashboardActivityDetails, apiDashboardBulkDetails, apiDashboardBulkPrint
    cmd/server/main.go — routes() function
    cmd/server/auth.go — requireRole, requireAPIRole, ctxUser
    .env.example — current content
  </read_first>
  <action>
    Append to `cmd/server/main_test.go` with section comments:

    `// ---- Admin Handler Tests ----`:

    1. `TestAdminPageAuthenticated(t *testing.T)`:
       - `app := testApp(t)`, seed sysadmin user
       - Set ctxUser in request context
       - Call `app.adminPage(rec, req)`
       - Assert rec.Code == 200, Content-Type text/html

    2. `TestAdminUsersSection(t *testing.T)`:
       - Seed users, set ctxUser context
       - Call `app.adminUsersSection(rec, req)`
       - Assert rec.Code == 200, HTML content

    3. `TestAdminCreateUser(t *testing.T)`:
       - Seed sysadmin user, set ctxUser context
       - POST form data: `username=test_new_user&password=password123&role=conferente`
       - Call `app.adminCreateUser(rec, req)` (it reads form values with ParseForm)
       - Assert rec.Code == 200, response renders users_section template (no redirect)
       - Verify user exists: query `SELECT COUNT(*) FROM users WHERE username='test_new_user'`
       
    4. `TestAdminEditUserRow(t *testing.T)`:
       - Seed user with known ID
       - `httptest.NewRequest("GET", "/admin/users/{id}/edit", nil)` — use PathValue
       - Actually: since we call `app.adminEditUserRow(rec, req)` directly, we must use `r.SetPathValue("id", strconv.Itoa(user.ID))`
       - Assert rec.Code == 200, HTML content

    5. `TestAdminUserRow(t *testing.T)`:
       - Same pattern as edit user row but for adminUserRow

    6. `TestAdminUpdateUser(t *testing.T)`:
       - Seed admin user as currentUser (ctxUser), seed target user to update
       - POST form data: `role=gerente&password=`
       - Assert rec.Code == 200, renders user_row template

    `// ---- Dashboard Handler Tests ----`:

    7. `TestDashboardPageAuthenticated(t *testing.T)`:
       - Seed gerente user, set ctxUser context
       - Seed an activity so listActivities returns data
       - Call `app.dashboardPage(rec, req)`
       - Assert rec.Code == 200, HTML content

    8. `TestDashboardTable(t *testing.T)`:
       - Seed activity, set ctxUser context
       - Call `app.dashboardTable(rec, req)`
       - Assert rec.Code == 200, HTML content (activities_table partial)

    `// ---- API Admin Handler Tests ----`:

    9. `TestAPIAdminUsersList(t *testing.T)`:
       - Seed users
       - Call `app.apiAdminUsersList(rec, req, sysadminUser)`
       - Assert rec.Code == 200, JSON array with APIUser objects

    10. `TestAPIAdminUserCreate(t *testing.T)`:
        - POST JSON body with username/password/role
        - Assert rec.Code == 200, body["message"] == "OK"

    11. `TestAPIAdminUserUpdate(t *testing.T)`:
        - Seed target user, PATCH JSON body with role
        - Call `app.apiAdminUserUpdate(rec, req, sysadminUser)`
        - Assert rec.Code == 200

    `// ---- API Dashboard Handler Tests ----`:

    12. `TestAPIDashboardActivities(t *testing.T)`:
        - Seed activity
        - Assert rec.Code == 200, JSON array of APIActivity

    13. `TestAPIDashboardActivityDetails(t *testing.T)`:
        - Seed activity + produto_verificacao records
        - Assert rec.Code == 200, JSON with id/empresa/username/items

    14. `TestAPIDashboardBulkDetails(t *testing.T)`:
        - Seed 2 activities
        - POST JSON with ids array
        - Assert rec.Code == 200, JSON array

    15. `TestAPIDashboardBulkPrint(t *testing.T)`:
        - Seed 2 activities with impresso=false
        - POST JSON with ids array
        - Assert rec.Code == 200
        - Verify atividades set impresso=true

    For PathValue-based handlers (adminEditUserRow, adminUserRow, apiAdminUserUpdate, apiDashboardActivityDetails):
    - Use `req.SetPathValue("id", strconv.Itoa(id))` (available in Go 1.22+ with net/http)

    `// ---- Route Integration Tests ----`:

    16. `TestRoutesReturnCorrectStatus(t *testing.T)`:
        - Build full mux via `app.routes(mux)` (not through middleware chain)
        - Use testApp for DB
        - Table-driven tests for public routes (no auth):
          - GET /api/health → 200
          - GET / → 302 (redirects to /login)
          - GET /login → 200
          - GET /style.css → 200
          - GET /admin.css → 200
          - GET /htmx.min.js → 200
          - GET /shared.js → 200
          - POST /api/auth/login (invalid body) → 400 (bad JSON)

        - Then seed a user, create token, test authenticated routes:
          - GET /home → 200
          - GET /atividades → 200
          - GET /api/auth/me → 200

        Use `mux.ServeHTTP(rec, req)` to go through route matching.

    **Update `.env.example`**:
    Add at the end of `.env.example`:
    ```
    # For full test coverage (optional — tests skip gracefully when unset):
    # TEST_POSTGRES_URL=postgres://user:password@localhost:5432/go_simp_test?sslmode=disable
    ```

    `// ---- Coverage Gate ----`:

    17. `TestCoverageMeetsTarget(t *testing.T)`:
        - This test runs `go test -cover` and parses output OR just documents the coverage target
        - Since we can't easily run `go test -cover` from within a test, add a comment-only reference:
          ```go
          func TestCoverageMeetsTarget(t *testing.T) {
              t.Log("Coverage target: ≥70%. Run: go test -cover -count=1 ./cmd/server")
              // This is a documentation gate. Manual verification step.
          }
          ```
        - The actual coverage verification is done post-phase via:
          ```bash
          go test -coverprofile=/tmp/c.out -count=1 ./cmd/server
          go tool cover -func=/tmp/c.out
          ```

    Each handler test should set up the correct context (ctxUser), call the handler directly, and assert status code + response shape.
    For handlers that call requireAPIRole (those with *User parameter), pass the user directly.
    For handlers wrapped in requireRole (those without *User), set the user in context via `context.WithValue(r.Context(), ctxUser, user)`.

    Use unique usernames across ALL tests in this plan (prefix-based: "test_admin_page_1", "test_dashboard_1", etc.).
  </action>
  <verify>
    <automated>go test -run 'TestAdmin|TestDashboard|TestAPIAdmin|TestAPIDashboard|TestRoutes|TestCoverage' ./cmd/server -v -count=1 2>&1 || echo "SKIPPED: set TEST_POSTGRES_URL to run"</automated>
    <automated>go test -cover -count=1 ./cmd/server 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - 17 test functions added (6 admin + 2 dashboard + 3 API admin + 4 API dashboard + 1 route integration + 1 coverage gate)
    - .env.example documents TEST_POSTGRES_URL
    - Admin CRUD handlers tested: create, read, update user
    - Dashboard handlers tested: page + table rendering
    - API dashboard endpoints return correct JSON shapes
    - Route integration test covers public and authenticated routes through mux
    - `go test -cover -count=1 ./cmd/server` shows ≥ 70% coverage
    - All tests pass (or skip gracefully) on `go test ./cmd/server -count=1`
  </acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase Produces

### Files Modified

| File | Change |
|------|--------|
| `.env.example` | Added TEST_POSTGRES_URL documentation |

### Test Functions Created

| Symbol | File | Coverage Target |
|--------|------|-----------------|
| `TestLoginPostSuccess` | main_test.go | loginPost success path |
| `TestLoginPostWrongPassword` | main_test.go | loginPost auth failure |
| `TestLoginPostRateLimited` | main_test.go | loginPost rate limiting |
| `TestLogoutHandler` | main_test.go | logout handler |
| `TestAPILoginSuccess` | main_test.go | apiLogin success |
| `TestAPILoginInvalidCredentials` | main_test.go | apiLogin 401 |
| `TestAPILoginRateLimited` | main_test.go | apiLogin 429 |
| `TestAPILogout` | main_test.go | apiLogout |
| `TestAPIMeAuthenticated` | main_test.go | apiMe with auth |
| `TestAtividadesPageAuthenticated` | main_test.go | atividadesPage |
| `TestListActivities` | main_test.go | listActivities |
| `TestListFilterOptions` | main_test.go | listFilterOptions |
| `TestActivityDetailsData` | main_test.go | activityDetailsData |
| `TestAPILastInfoNoData` | main_test.go | apiLastInfo null |
| `TestAPILastInfoWithData` | main_test.go | apiLastInfo with data |
| `TestAPIFinalizarSuccess` | main_test.go | apiFinalizar success |
| `TestAPIFinalizarMissingFields` | main_test.go | apiFinalizar validation |
| `TestAPIFinalizarWithReadProducts` | main_test.go | apiFinalizar products |
| `TestAdminPageAuthenticated` | main_test.go | adminPage |
| `TestAdminUsersSection` | main_test.go | adminUsersSection |
| `TestAdminCreateUser` | main_test.go | adminCreateUser |
| `TestAdminEditUserRow` | main_test.go | adminEditUserRow |
| `TestAdminUserRow` | main_test.go | adminUserRow |
| `TestAdminUpdateUser` | main_test.go | adminUpdateUser |
| `TestDashboardPageAuthenticated` | main_test.go | dashboardPage |
| `TestDashboardTable` | main_test.go | dashboardTable |
| `TestAPIAdminUsersList` | main_test.go | apiAdminUsersList |
| `TestAPIAdminUserCreate` | main_test.go | apiAdminUserCreate |
| `TestAPIAdminUserUpdate` | main_test.go | apiAdminUserUpdate |
| `TestAPIDashboardActivities` | main_test.go | apiDashboardActivities |
| `TestAPIDashboardActivityDetails` | main_test.go | apiDashboardActivityDetails |
| `TestAPIDashboardBulkDetails` | main_test.go | apiDashboardBulkDetails |
| `TestAPIDashboardBulkPrint` | main_test.go | apiDashboardBulkPrint |
| `TestRoutesReturnCorrectStatus` | main_test.go | route integration |
| `TestCoverageMeetsTarget` | main_test.go | coverage documentation gate |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Handler HTTP I/O | Requests enter handler via httptest, responses captured before network |
| Test DB write operations | adminCreateUser, adminUpdateUser, apiFinalizar write to test DB — all cleaned up |
| Route integration mux | Full routes() mux tested without middleware chain (csrfMiddleware skipped in direct handler tests) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | Tampering | adminCreateUser handler | mitigate | TestAdminCreateUser asserts user is inserted into DB. TestAPIAdminUserCreate asserts 200 response. Combined with requireAPIRole("sysadmin") tested in Plan 03 |
| T-06-16 | Tampering | apiFinalizar handler | mitigate | TestAPIFinalizarSuccess asserts activity + enderecos inserted. TestAPIFinalizarMissingFields asserts 400 on invalid input. Transaction rollback verified by defer tx.Rollback() |
| T-06-17 | Information Disclosure | Route integration | accept | TestRoutesReturnCorrectStatus asserts public routes return expected status codes. No sensitive data exposed beyond what production would show |
| T-06-18 | Denial of Service | Rate limiting | mitigate | TestLoginPostRateLimited and TestAPILoginRateLimited assert rate limiter blocks after 5 attempts — validates both page and API endpoints |
| T-06-19 | Information Disclosure | HandleError page path | mitigate | TestLoginPostWrongPassword asserts login page shows user-facing error, not raw error text or stack trace |
| T-06-20 | Privilege Escalation | Admin user update self-protection | accept | TestAdminUpdateUser in this plan tests happy path. Self-edit prevention (id == currentUser.ID → 400) is tested implicitly — explicit case added in plan |
| T-06-SC | Tampering | No new packages | mitigate | All tests use Go stdlib + existing go.mod deps. `go mod verify` confirms integrity |
</threat_model>

<verification>
## Plan-Level Verification

| Check | Command |
|-------|---------|
| Auth handler tests | `go test -run 'TestLoginPost\|TestLogoutHandler\|TestAPILogin\|TestAPILogout\|TestAPIMe\|TestAtividadesPage' ./cmd/server -v -count=1` |
| Activity handler tests | `go test -run 'TestListActivities\|TestListFilterOptions\|TestActivityDetailsData\|TestAPILastInfo\|TestAPIFinalizar' ./cmd/server -v -count=1` |
| Admin/dashboard handler tests | `go test -run 'TestAdmin\|TestDashboard\|TestAPIAdmin\|TestAPIDashboard' ./cmd/server -v -count=1` |
| Route integration | `go test -run TestRoutes ./cmd/server -v -count=1` |
| Full suite (no DB) | `go test ./cmd/server -count=1` |
| Full suite (with DB) | `TEST_POSTGRES_URL=postgres://... go test ./cmd/server -count=1` |
| Coverage gate | `go test -cover -count=1 ./cmd/server` shows ≥ 70% |
| Per-function coverage | `go test -coverprofile=/tmp/c.out -count=1 ./cmd/server && go tool cover -func=/tmp/c.out` |
| Build | `go build ./cmd/server` |
</verification>

<success_criteria>
- [ ] 35 test functions added across all handler groups
- [ ] `go test ./cmd/server -count=1` passes (DB tests skip gracefully)
- [ ] `TEST_POSTGRES_URL=postgres://... go test ./cmd/server -count=1` passes all tests
- [ ] `go test -cover -count=1 ./cmd/server` shows ≥ 70% coverage
- [ ] .env.example documents TEST_POSTGRES_URL
- [ ] Route integration test covers public and authenticated route status codes
- [ ] All handler success and error paths tested
</success_criteria>

<output>
Create `.planning/phases/06-testing-infrastructure/06-04-SUMMARY.md` when done
</output>
