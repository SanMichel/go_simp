---
phase: 06-testing-infrastructure
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - cmd/server/main_test.go
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
user_setup:
  - service: Postgres (test database)
    why: "Auth middleware and session tests require a real Postgres connection (currentUser calls findUserByID)"
    env_vars:
      - name: TEST_POSTGRES_URL
        value: "postgres://user:password@localhost:5432/go_simp_test?sslmode=disable"
        source: "Your local Postgres instance or docker-compose"
    notes: "Without TEST_POSTGRES_URL, all tests in this plan skip gracefully. `go test` still passes."
must_haves:
  truths:
    - "requireAPIRole returns 401 for unauthenticated requests"
    - "requireAPIRole returns 403 for authenticated requests with insufficient role"
    - "requireAPIRole calls the next handler for authorized requests"
    - "requireRole redirects unauthenticated requests to /login"
    - "requireRole redirects unauthorized roles to role-appropriate pages"
    - "requireRole calls the next handler for authorized requests"
    - "currentUser returns User for valid, non-expired, non-revoked tokens"
    - "currentUser returns error for invalid signature (bad token)"
    - "currentUser returns error for expired tokens"
    - "revokeSession updates last_token_at for a user"
    - "redirectByRole redirects conferente→/atividades, gerente→/dashboard, sysadmin→/admin"
    - "findUserByUsername returns correct user for existing username"
    - "findUserByUsername returns sql.ErrNoRows for nonexistent username"
    - "findUserByID returns correct user for existing ID"
    - "findUserByID returns sql.ErrNoRows for nonexistent ID"
    - "listUsers returns all users ordered by id"
  artifacts:
    - path: cmd/server/main_test.go
      provides: "Auth middleware integration tests, DB query tests"
      min_lines: 1400
  key_links:
    - from: "main_test.go"
      to: "testhelper.go"
      via: "calls testApp(), testUser(), testToken(), cleanupTestData()"
    - from: "main_test.go"
      to: "auth.go"
      via: "tests requireRole, requireAPIRole, currentUser, revokeSession, redirectByRole"
    - from: "main_test.go"
      to: "db.go"
      via: "tests findUserByUsername, findUserByID, listUsers"
---

<objective>
**Phase 6, Plan 03: Auth Integration and DB Query Tests**

Add database-dependent tests for auth middleware (requireRole, requireAPIRole), session management (currentUser, revokeSession, redirectByRole), and Postgres query functions (findUserByUsername, findUserByID, listUsers). All tests use `testApp()` which requires `TEST_POSTGRES_URL`. Tests skip gracefully when the env var is unset.

**Purpose:** Cover auth middleware code paths that call `currentUser` → `findUserByID` (DB-dependent), session lifecycle from creation to revocation, and core data access functions. These are the foundation for handler tests in Plan 04.

**Output:** Expanded `cmd/server/main_test.go` with ~12 additional test functions, gated by `TEST_POSTGRES_URL`.
</objective>

<execution_context>
@/home/bblosangeles/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/bblosangeles/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-testing-infrastructure/06-RESEARCH.md
@cmd/server/auth.go
@cmd/server/db.go
@cmd/server/handlers.go
@cmd/server/main_test.go
@cmd/server/models.go
@cmd/server/testhelper.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add requireRole and requireAPIRole integration tests</name>
  <files>cmd/server/main_test.go</files>
  <read_first>
    cmd/server/auth.go — requireRole (lines 86-110), requireAPIRole (lines 112-131)
    cmd/server/testhelper.go — testApp, testUser, testToken function signatures
    cmd/server/errors.go — ErrCodeUnauthorized (401), ErrCodeForbidden (403) definitions
    .planning/phases/06-testing-infrastructure/06-RESEARCH.md — Pattern 4 (auth middleware tests)
  </read_first>
  <action>
    Append to `cmd/server/main_test.go` with section comment `// ---- Auth Middleware Integration Tests ----`.

    These tests call `testApp(t)` which skips if `TEST_POSTGRES_URL` is unset. Each test seeds its own test user via `testUser()`.

    1. `TestRequireRoleUnauthenticated(t *testing.T)`:
       - `app := testApp(t)`
       - `handler := app.requireRole("gerente,sysadmin", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) { w.WriteHeader(http.StatusOK) }))`
       - Request with NO cookie (unauthenticated)
       - Assert rec.Code is 302 (Found — redirects to /login)

    2. `TestRequireRoleAllowedRole(t *testing.T)`:
       - `app := testApp(t)`
       - Seed user: `testUser(t, app, "test_gerente_role", "gerente", "pass1234")` — use unique username
       - Create token: `testToken(app, user.ID)`
       - `handler := app.requireRole("gerente,sysadmin", http.HandlerFunc(...200...))`
       - Request with valid token cookie
       - Assert rec.Code == 200

    3. `TestRequireRoleForbiddenRoleRedirects(t *testing.T)`:
       - Seed conferente user
       - `handler := app.requireRole("gerente,sysadmin", ...)`
       - Assert rec.Code is 302 (redirects conferente→/atividades)

    4. `TestRequireAPIRoleUnauthenticated(t *testing.T)`:
       - `app := testApp(t)`
       - `handler := app.requireAPIRole("sysadmin", nil)` — next handler should NOT be called
       - Request with NO cookie
       - Assert rec.Code == 401

    5. `TestRequireAPIRoleForbiddenRole(t *testing.T)`:
       - Seed conferente user
       - `handler := app.requireAPIRole("sysadmin", nil)`
       - Request with valid token cookie for conferente
       - Assert rec.Code == 403

    6. `TestRequireAPIRoleAllowedRole(t *testing.T)`:
       - Seed sysadmin user
       - `handler := app.requireAPIRole("sysadmin", func(w http.ResponseWriter, r *http.Request, u *User) { w.WriteHeader(http.StatusOK) })`
       - Request with valid token cookie
       - Assert rec.Code == 200
       - Assert *User passed to next handler has correct Role

    7. `TestRequireAPIRoleEmptyRoles(t *testing.T)`:
       - `app.requireAPIRole("", nextHandler)` with empty roles string
       - any authenticated user passes (empty allowed map)
       - Seed conferente user, valid token → assert 200

    Each test user must have a unique username (use `t.Name()` sanitized or a counter prefix like `test_reqrole_1`).
    The test helper cleanupDataTest removes records with `username LIKE 'test_%'`.
    Use `t.Helper()` inline on test functions, not on anonymous funcs.

    Import `net/http` httptest as needed (already imported in main_test.go).
  </action>
  <verify>
    <automated>go test -run 'TestRequireRole|TestRequireAPI' ./cmd/server -v -count=1 2>&1 || echo "SKIPPED: set TEST_POSTGRES_URL to run"</automated>
  </verify>
  <acceptance_criteria>
    - 7 test functions added (3 requireRole + 4 requireAPIRole)
    - Tests call testApp() which skips gracefully when TEST_POSTGRES_URL is unset
    - requireAPIRole returns 401/403/200 for unauthenticated/forbidden/allowed
    - requireRole returns 302 redirect for unauthenticated and forbidden
    - Each test user has unique username to avoid PK conflicts
    - All pass when TEST_POSTGRES_URL is set
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add currentUser, revokeSession, and redirectByRole tests</name>
  <files>cmd/server/main_test.go</files>
  <read_first>
    cmd/server/auth.go — currentUser (lines 21-60), makeToken (lines 62-77), revokeSession (lines 152-154), redirectByRole (lines 132-141)
    cmd/server/testhelper.go — testApp, testUser, testToken
    cmd/server/main_test.go — existing TestMakeToken for token format reference
  </read_first>
  <action>
    Append to `cmd/server/main_test.go` with section comment `// ---- Session Management Tests ----`.

    1. `TestCurrentUserValidToken(t *testing.T)`:
       - `app := testApp(t)`
       - Seed user: `testUser(t, app, "test_currentuser_valid", "conferente", "pass")`
       - Create token: `testToken(app, user.ID)`
       - Build request with token cookie, call `app.currentUser(r)`
       - Assert returned user is not nil, user.ID == user.ID, user.Username == "test_currentuser_valid"

    2. `TestCurrentUserBadSignature(t *testing.T)`:
       - `app := testApp(t)`
       - Create a token with a DIFFERENT secret (wrong secret), then set cookie with that token
       - Create second app `app2` with `SessionSecret: []byte("different-secret-thats-also-32-chars!!")`
       - Use `testToken(app2, 1)` to generate token signed with wrong secret
       - Call `app.currentUser(r)` — should return error (bad signature)

    3. `TestCurrentUserExpiredToken(t *testing.T)`:
       - `app := testApp(t)` — SessionTTL is 8h
       - Generate a token manually with `Exp` set to `time.Now().Add(-1 * time.Hour).Unix()` (1 hour in the past)
       - Use direct token construction (not testToken, which uses app.cfg.SessionTTL)
       - Call `app.currentUser(r)` — should return error (expired)

    4. `TestCurrentUserRevokedSession(t *testing.T)`:
       - `app := testApp(t)`
       - Seed user, create token, call revokeSession for that user
       - Call `app.currentUser(r)` — should return error (session revoked)
       - Assert error message contains "revoked"

    5. `TestRevokeSession(t *testing.T)`:
       - Seed user, capture initial `last_token_at` (may be NULL/valid)
       - Call `app.revokeSession(ctx, user.ID)`
       - Re-query user; assert `last_token_at` is valid and recent

    6. `TestRedirectByRole(t *testing.T)`:
       - `&App{cfg: Config{...}}` (NO testApp needed — no DB dependency)
       - Table-driven: `{"conferente", "/atividades"}`, `{"gerente", "/dashboard"}`, `{"sysadmin", "/admin"}`
       - `httptest.NewRecorder`, `httptest.NewRequest`
       - `app.redirectByRole(rec, req, role)`
       - Assert `rec.Code == 302`, `rec.Header().Get("Location") == expected`

    For test 3 (expired token), construct token directly using the same HMAC/JSON pattern as testToken but with a past Exp value. Import `crypto/hmac`, `crypto/sha256`, `encoding/base64`, `encoding/json`.
  </action>
  <verify>
    <automated>go test -run 'TestCurrentUser|TestRevokeSession|TestRedirectByRole' ./cmd/server -v -count=1 2>&1 || echo "SKIPPED: set TEST_POSTGRES_URL to run"</automated>
  </verify>
  <acceptance_criteria>
    - 6 test functions added (4 currentUser + 1 revokeSession + 1 redirectByRole)
    - currentUser valid token returns correct user
    - currentUser bad signature returns error
    - currentUser expired token returns error
    - currentUser revoked session returns error
    - revokeSession updates last_token_at to recent timestamp
    - redirectByRole redirects to correct path per role (no DB needed)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add DB query function tests for findUserByUsername, findUserByID, listUsers</name>
  <files>cmd/server/main_test.go</files>
  <read_first>
    cmd/server/db.go — findUserByUsername (line 183), findUserByID (line 189), listUsers (line 195)
    cmd/server/models.go — User struct (fields: ID, Username, PasswordHash, Role, LastTokenAt)
    cmd/server/testhelper.go — testApp, testUser, cleanupTestData
  </read_first>
  <action>
    Append to `cmd/server/main_test.go` with section comment `// ---- DB Query Tests ----`.

    1. `TestFindUserByUsername(t *testing.T)`:
       - `app := testApp(t)`
       - Seed user: `testUser(t, app, "test_findbyuser", "conferente", "secret123")`
       - `u, err := app.findUserByUsername(ctx, "test_findbyuser")`
       - Assert err == nil, u.Username == "test_findbyuser", u.Role == "conferente"
       - Assert PasswordHash is non-empty (bcrypt hash)
       - Also test nonexistent: `_, err := app.findUserByUsername(ctx, "nonexistent")` — assert err == sql.ErrNoRows

    2. `TestFindUserByID(t *testing.T)`:
       - Seed user, capture the returned User struct (has DB-assigned ID)
       - `u, err := app.findUserByID(ctx, user.ID)`
       - Assert err == nil, u.ID == user.ID, u.Username == user.Username
       - Test nonexistent: `_, err := app.findUserByID(ctx, 999999)` — assert err == sql.ErrNoRows

    3. `TestListUsers(t *testing.T)`:
       - `app := testApp(t)` — this seeds the admin user via seedAdmin() in testApp
       - Call `users, err := app.listUsers(ctx)`
       - Assert err == nil
       - Assert at least 1 user exists (the admin user from seedAdmin)
       - Seed 2 more test users with unique usernames
       - Call listUsers again, assert count increased by 2
       - Assert users are ordered by id (ascending)
       - Assert each UserRow does NOT contain PasswordHash (listUsers selects id, username, role, last_token_at)

    Use `context.Background()` for query contexts.
    Each test user must have a unique username across all test functions in this plan.
    Use `t.Helper()` on test functions.
  </action>
  <verify>
    <automated>go test -run 'TestFindUser|TestListUsers' ./cmd/server -v -count=1 2>&1 || echo "SKIPPED: set TEST_POSTGRES_URL to run"</automated>
  </verify>
  <acceptance_criteria>
    - 3 test functions with comprehensive cases
    - findUserByUsername returns User for existing, sql.ErrNoRows for nonexistent
    - findUserByID returns User for existing, sql.ErrNoRows for nonexistent
    - listUsers returns all users ordered by id ascending
    - listUsers UserRow does not contain password field
    - All skip gracefully when TEST_POSTGRES_URL is unset
  </acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase Produces

### Test Functions Created

| Symbol | File | Coverage Target |
|--------|------|-----------------|
| `TestRequireRoleUnauthenticated` | main_test.go | requireRole redirect on no auth |
| `TestRequireRoleAllowedRole` | main_test.go | requireRole pass-through |
| `TestRequireRoleForbiddenRoleRedirects` | main_test.go | requireRole redirect on wrong role |
| `TestRequireAPIRoleUnauthenticated` | main_test.go | requireAPIRole 401 |
| `TestRequireAPIRoleForbiddenRole` | main_test.go | requireAPIRole 403 |
| `TestRequireAPIRoleAllowedRole` | main_test.go | requireAPIRole pass-through |
| `TestRequireAPIRoleEmptyRoles` | main_test.go | requireAPIRole any auth user |
| `TestCurrentUserValidToken` | main_test.go | currentUser success path |
| `TestCurrentUserBadSignature` | main_test.go | currentUser HMAC failure |
| `TestCurrentUserExpiredToken` | main_test.go | currentUser expiry check |
| `TestCurrentUserRevokedSession` | main_test.go | currentUser revocation check |
| `TestRevokeSession` | main_test.go | revokeSession writes to DB |
| `TestRedirectByRole` | main_test.go | redirectByRole (no DB) |
| `TestFindUserByUsername` | main_test.go | findUserByUsername |
| `TestFindUserByID` | main_test.go | findUserByID |
| `TestListUsers` | main_test.go | listUsers |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test DB user seed | testUser creates users with known passwords — cleanup removes them |
| Token lifecycle | Expired/revoked tokens rejected by currentUser — verified by tests |
| Role bypass | RequireAPIRole/requireRole must block unauthorized roles — verified by tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Elevation of Privilege | requireAPIRole | mitigate | TestRequireAPIRoleForbiddenRole asserts 403 on wrong role. TestRequireAPIRoleUnauthenticated asserts 401 on no auth |
| T-06-11 | Elevation of Privilege | requireRole | mitigate | TestRequireRoleForbiddenRoleRedirects asserts redirect on wrong role. TestRequireRoleUnauthenticated asserts redirect on no auth |
| T-06-12 | Spoofing | currentUser | mitigate | TestCurrentUserBadSignature asserts invalid HMAC rejected. TestCurrentUserExpiredToken asserts expired rejected. TestCurrentUserRevokedSession asserts revoked rejected |
| T-06-13 | Repudiation | revokeSession | mitigate | TestRevokeSession asserts last_token_at updated after call |
| T-06-14 | Tampering | Test DB data | mitigate | cleanupTestData removes all test records in t.Cleanup(). Each test uses unique usernames to avoid collisions |
| T-06-SC | Tampering | No new packages | mitigate | All tests use Go stdlib + existing go.mod deps. `go mod verify` confirms integrity |
</threat_model>

<verification>
## Plan-Level Verification

| Check | Command |
|-------|---------|
| Auth middleware integration | `go test -run 'TestRequireRole\|TestRequireAPI' ./cmd/server -v -count=1` |
| Session management | `go test -run 'TestCurrentUser\|TestRevokeSession\|TestRedirectByRole' ./cmd/server -v -count=1` |
| DB query functions | `go test -run 'TestFindUser\|TestListUsers' ./cmd/server -v -count=1` |
| Full existing suite | `go test ./cmd/server -count=1` |
| Build check | `go build ./cmd/server` |
</verification>

<success_criteria>
- [ ] 16 new test functions covering auth middleware, session management, and DB queries
- [ ] requireRole/requireAPIRole tested with unauthenticated, wrong role, and allowed role cases
- [ ] currentUser tested with valid, bad signature, expired, and revoked cases
- [ ] revokeSession verified to update last_token_at
- [ ] findUserByUsername/findUserByID tested with existing and nonexistent cases
- [ ] All tests skip gracefully (t.Skip) when TEST_POSTGRES_URL is unset — `go test -count=1 ./cmd-server` still passes
- [ ] ~10-15% coverage gain from auth.go + db.go paths
</success_criteria>

<output>
Create `.planning/phases/06-testing-infrastructure/06-03-SUMMARY.md` when done
</output>
