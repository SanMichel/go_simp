---
phase: 08-es5-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/server/templates/atividades/atividades-login.html
  - cmd/server/templates/atividades/atividades.html
  - cmd/server/templates/atividades/atividades-utils.js
  - cmd/server/templates/atividades/atividades-login.js
  - cmd/server/templates/atividades/atividades-scan.js
  - cmd/server/templates/atividades/atividades-consulta.js
  - cmd/server/templates/shared.js
autonomous: true
requirements: [ES5-01]
decisions_addressed: [D-01, D-03, D-04, D-05, D-08, D-09, D-10, D-16, D-17, D-18]

must_haves:
  truths:
    - "atividades/ directory exists with all 6 files (2 HTML + 4 JS)"
    - "New HTML templates use {{define}} blocks with correct template names"
    - "atividades-login.html is a standalone login page with form, error slot, and script tags for atividades-utils.js + atividades-login.js"
    - "atividades.html is the main SPA template with 5 screens (start, scanning, predio-switch, consulta, report), product-detail modal, no login screen, no reauth modal, script tags for atividades-utils.js + atividades-scan.js + atividades-consulta.js + htmx.min.js"
    - "atividades-utils.js exports ES5-compatible versions of apiCall/XHR wrapper, apiGet, apiPost, showLoader, formatDate, playBeep, escHtml, sanitizeHtml"
    - "atividades-login.js handles form submit via XHR to /api/auth/login, redirects to /atividades on success, shows error on failure"
    - "atividades-scan.js contains full scanning state machine converted to ES5: state management, screen navigation, EAN scan flow, building switch, finalize, logout"
    - "atividades-consulta.js contains product consultation (by code and by description) converted to ES5"
    - "All 4 JS files use zero ES6+ features: no const/let, no arrow functions, no async/await, no fetch, no template literals, no optional chaining, no for...of"
    - "shared.js has COPIED comments above each ported function"
    - "D-01: atividades split into standalone templates/atividades/ directory"
    - "D-03: Two HTML templates created (atividades-login.html, atividades.html)"
    - "D-04: Four JS files created (atividades-login.js, atividades-scan.js, atividades-consulta.js, atividades-utils.js)"
    - "D-05: Utilities copied from shared.js into atividades-utils.js with COPIED comments"
    - "D-08: Existing login.js left unchanged — only new atividades files created"
    - "D-09: All 4 JS files written directly in ES5 — no Babel, no build step"
    - "D-10: ES5 constraints enforced: var, function(){}, XHR, string concat, && guards, index loops"
    - "D-16: No hard size target — clean ES5 is the goal"
    - "D-17: No minification — readability prioritized over wire size"
    - "D-18: No CSS changes — existing stylesheets reused"
  artifacts:
    - path: "cmd/server/templates/atividades/atividades-login.html"
      provides: "Standalone login template for /atividades/login route"
      contains: "{{define \"atividades-login\"}}"
    - path: "cmd/server/templates/atividades/atividades.html"
      provides: "Main scanning SPA template for /atividades route"
      contains: "{{define \"atividades\"}}"
    - path: "cmd/server/templates/atividades/atividades-utils.js"
      provides: "ES5 shared utilities (XHR wrapper, sanitize, date, audio)"
      exports: ["apiCall", "apiGet", "apiPost", "showLoader", "formatDate", "playBeep", "escHtml", "sanitizeHtml"]
    - path: "cmd/server/templates/atividades/atividades-login.js"
      provides: "Login form handler (ES5 XHR-based)"
    - path: "cmd/server/templates/atividades/atividades-scan.js"
      provides: "Scanning workflow state machine (ES5 XHR-based)"
    - path: "cmd/server/templates/atividades/atividades-consulta.js"
      provides: "Product consultation (ES5 XHR-based)"
    - path: "cmd/server/templates/shared.js"
      provides: "COPIED comments above ported functions for traceability"
  key_links:
    - from: "templates/atividades/atividades-login.html"
      to: "/atividades-utils.js"
      via: "<script src=\"/atividades-utils.js\">"
    - from: "templates/atividades/atividades-login.html"
      to: "/atividades-login.js"
      via: "<script src=\"/atividades-login.js\">"
    - from: "templates/atividades/atividades.html"
      to: "/atividades-utils.js"
      via: "<script src=\"/atividades-utils.js\">"
    - from: "templates/atividades/atividades.html"
      to: "/atividades-scan.js"
      via: "<script src=\"/atividades-scan.js\">"
    - from: "templates/atividades/atividades.html"
      to: "/atividades-consulta.js"
      via: "<script src=\"/atividades-consulta.js\">"
    - from: "templates/atividades/atividades.html"
      to: "/htmx.min.js"
      via: "<script src=\"/htmx.min.js\">"
    - from: "atividades-scan.js XHR calls"
      to: "atividades-utils.js apiCall/apiGet/apiPost"
      via: "global function calls (ES5: var-based, no imports)"
---

<objective>
Create the standalone `templates/atividades/` directory with 2 HTML templates and 4 ES5-compatible JS files that replace the monolithic app.js for the scanning workflow, and mark ported functions in shared.js.

Purpose: Split the scanning workflow frontend into a self-contained `templates/atividades/` directory with ES5-only JS files that run on any browser (including legacy warehouse browsers). The new directory contains everything the /atividades page needs — no shared.js or app.js dependency.

Output: 6 new files in `templates/atividades/` + commented shared.js
</objective>

<execution_context>
@/home/bblosangeles/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/bblosangeles/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-es5-compatibility/08-RESEARCH.md
@.planning/phases/08-es5-compatibility/08-PATTERNS.md
@.planning/phases/08-es5-compatibility/08-CONTEXT.md
@.planning/phases/08-es5-compatibility/08-UI-SPEC.md

# Existing files to read before touching
@cmd/server/templates/shared.js
@cmd/server/templates/app.js
@cmd/server/templates/atividades.html
@cmd/server/templates/login.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create templates/atividades/ directory and both HTML templates</name>
  <files>
    cmd/server/templates/atividades/atividades-login.html
    cmd/server/templates/atividades/atividades.html
  </files>
  <read_first>
    @cmd/server/templates/login.html — reference for isolated login template structure
    @cmd/server/templates/atividades.html — source to derive new template from (remove screen-login, modal-reauth, change scripts)
    .planning/phases/08-es5-compatibility/08-PATTERNS.md — §`templates/atividades/atividades-login.html` and §`templates/atividades/atividades.html` for exact patterns
    .planning/phases/08-es5-compatibility/08-UI-SPEC.md — §Copywriting Contract for exact copy values
  </read_first>
  <action>
    Create directory `cmd/server/templates/atividades/`.
    Create `atividades-login.html` (analog: `login.html`, per PATTERNS.md §`templates/atividades/atividades-login.html`):
    - Template name: `{{define "atividades-login"}}`
    - Title: "SIMP — Atividades Login"
    - Link stylesheet: `/style.css` (not `/admin.css`)
    - Loader div with id="loader" class="hidden" with spinner
    - app-container → screen → centered login card
    - Heading: "Acesso Atividades" (H2), subtitle: "Faça login para iniciar" (P)
    - Form id="form-atividades-login" (NOT "form-login-page" or "form-login" to avoid ID conflicts)
    - Input fields: username (id="login-username") and password (id="login-password") with proper labels and placeholders per UI-SPEC.md §Copywriting
    - Error element: `p id="login-error" class="text-red-500 text-sm text-center hidden"`
    - Submit button: "Entrar" with btn btn-primary classes
    - Script tags at bottom: `/atividades-utils.js` then `/atividades-login.js` (in that order)
    - No header, no reauth modal, no HTMX script, no additional JS

    Create `atividades.html` in `templates/atividades/` (analog: existing `templates/atividades.html`, per PATTERNS.md §`templates/atividades/atividades.html`):
    - Template name: `{{define "atividades"}}` — intentionally same name as the existing `templates/atividades.html` template. The ParseFS ordering in main.go (templates/*.html loaded before templates/atividades/*.html) means this new template overwrites the old one at parse time, which is the intended behavior.
    - Copy the HTML structure from the existing `templates/atividades.html` but:
      - REMOVE `#screen-login` div (login is now served by `atividades-login.html`)
      - REMOVE `#modal-reauth` div (re-auth handled by redirect to `/atividades/login`)
      - KEEP screens: `screen-start`, `screen-scanning`, `screen-predio-switch`, `screen-consulta`, `screen-report`
      - KEEP `modal-product-detail` (unchanged)
      - CHANGE script tags at bottom from `/shared.js` + `/app.js` to:
        `/atividades-utils.js` → `/atividades-scan.js` → `/atividades-consulta.js` → `/htmx.min.js`
      - All other content, classes, IDs, and data attributes must remain IDENTICAL to the existing template
    - The HTML title remains "SIMP — Atividades"
  </action>
  <verify>
    <automated>ls cmd/server/templates/atividades/atividades-login.html cmd/server/templates/atividades/atividades.html && grep -q '{{define "atividades-login"}}' cmd/server/templates/atividades/atividades-login.html && grep -q '{{define "atividades"}}' cmd/server/templates/atividades/atividades.html && grep -q '/atividades-utils.js' cmd/server/templates/atividades/atividades.html && grep -q '/atividades-scan.js' cmd/server/templates/atividades/atividades.html</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/server/templates/atividades/` directory exists
    - `atividades-login.html` exists with template name "atividades-login", form id="form-atividades-login", script tags for atividades-utils.js and atividades-login.js
    - `atividades.html` exists with template name "atividades", 5 screens (no login, no reauth), 4 script tags (atividades-utils.js, atividades-scan.js, atividades-consulta.js, htmx.min.js)
    - Both files pass `ls` check and content grep
  </acceptance_criteria>
  <done>Both HTML templates exist in templates/atividades/ with correct template names, script loads, and screen structure</done>
</task>

<task type="auto">
  <name>Task 2: Create atividades-utils.js (ES5 XHR wrapper + ported utilities) and atividades-login.js (ES5 login form)</name>
  <files>
    cmd/server/templates/atividades/atividades-utils.js
    cmd/server/templates/atividades/atividades-login.js
  </files>
  <read_first>
    @cmd/server/templates/shared.js — source for ported functions (apiCall pattern, showLoader, formatDate, playBeep, escHtml, sanitizeHtml)
    @cmd/server/templates/app.js — reference for login flow patterns (lines 443-567)
    @cmd/server/templates/login.js — analog for login form handling
    .planning/phases/08-es5-compatibility/08-PATTERNS.md — §`templates/atividades/atividades-utils.js` and §`templates/atividades/atividades-login.js` for exact ES5 implementations
    .planning/phases/08-es5-compatibility/08-RESEARCH.md — §XHR Wrapper Design and §CSRF Token Reading for apiCall patterns
  </read_first>
  <action>
    Create `atividades-utils.js` (analog: shared.js, per PATTERNS.md §`templates/atividades/atividades-utils.js`). All functions must be top-level `function` declarations with `var` only. Include:
    - `function apiCall(method, url, body, onSuccess, onUnauthorized)` — XHR-based API call wrapper (per PATTERNS.md apiCall pattern lines 95-131). Must:
      * Use `var xhr = new XMLHttpRequest()`
      * Set `xhr.onreadystatechange` BEFORE `xhr.open()` (Pitfall 4 from RESEARCH.md)
      * Set `xhr.withCredentials = true` after open but before send
      * Set Content-Type header to "application/json"
      * For POST/PATCH/DELETE: extract CSRF token from cookie using `document.cookie.match(/(?:^|;\s*)csrf_token=([^;]*)/)` and set X-CSRF-Token header (per RESEARCH.md §CSRF Token Reading)
      * On 401: call `onUnauthorized()` if provided
      * On success: parse JSON response, call `onSuccess(ok, status, data)` where `ok = xhr.status >= 200 && xhr.status < 300`
      * On non-JSON response: wrap as `{ error: responseText }`
    - `function apiGet(url, onSuccess, onUnauthorized)` — convenience wrapper calling apiCall("GET", url, null, ...)
    - `function apiPost(url, body, onSuccess, onUnauthorized)` — convenience wrapper calling apiCall("POST", url, body, ...)
    - `function showLoader(show)` — copy verbatim from shared.js (already ES5, PATTERNS.md §showLoader)
    - `function formatDate(dateStr)` — copy from shared.js but convert the arrow-function pad to ES5: `function pad(n) { return n < 10 ? "0" + n : "" + n; }` and replace template literal with string concatenation (per RESEARCH.md §formatDate)
    - `function playBeep(type)` — copy verbatim from shared.js (already ES5, PATTERNS.md §playBeep)
    - `function escHtml(unsafe)` — copy verbatim from shared.js (already ES5, PATTERNS.md §escHtml)
    - `function sanitizeHtml(dirty)` — copy verbatim from shared.js (already ES5)

    Create `atividades-login.js` (analog: login.js, per PATTERNS.md §`templates/atividades/atividades-login.js`):
    - Single `document.addEventListener("DOMContentLoaded", function() { ... })` block with all `var` declarations at top
    - On load: `apiGet("/api/auth/me", function(ok, status, data) {...})` — if user data returned (ok && data.user), redirect to `/atividades` via `window.location.href = "/atividades"`
    - Bind submit handler on `document.getElementById("form-atividades-login")`:
      * `e.preventDefault()`
      * `showLoader(true)`
      * Read username/password from form fields
      * `apiPost("/api/auth/login", { username: ..., password: ... }, function(ok, status, data) {...})`
      * On success (ok && data.user): `window.location.href = "/atividades"`
      * On failure: show error text in `#login-error` element via `innerText` (NOT innerHTML)
      * `showLoader(false)` in both success and failure paths
    - All ES5 constraints per D-10: `var`, `function(){}`, no template literals, no arrow functions
    - The `onUnauthorized` callback for login POST should also show error text ("Sessão expirada") in `#login-error`
  </action>
  <verify>
    <automated>ls cmd/server/templates/atividades/atividades-utils.js cmd/server/templates/atividades/atividades-login.js && grep -q 'function apiCall' cmd/server/templates/atividades/atividades-utils.js && grep -q 'function escHtml' cmd/server/templates/atividades/atividades-utils.js && grep -q 'function playBeep' cmd/server/templates/atividades/atividades-utils.js && grep -q 'function formatDate' cmd/server/templates/atividades/atividades-utils.js && grep -q 'function showLoader' cmd/server/templates/atividades/atividades-utils.js && grep -q 'function sanitizeHtml' cmd/server/templates/atividades/atividades-utils.js && grep -q 'form-atividades-login' cmd/server/templates/atividades/atividades-login.js && grep -q 'apiPost' cmd/server/templates/atividades/atividades-login.js</automated>
  </verify>
  <acceptance_criteria>
    - atividades-utils.js contains all 8 exported functions (apiCall, apiGet, apiPost, showLoader, formatDate, playBeep, escHtml, sanitizeHtml), all as top-level function declarations
    - XHR apiCall sets onreadystatechange before open, handles CSRF for mutating requests, parses JSON responses, calls onSuccess(ok, status, data)
    - formatDate uses string concatenation, no template literals
    - atividades-login.js checks /api/auth/me on load, handles form submit via XHR to /api/auth/login, redirects to /atividades on success, shows error on failure
    - No ES6+ syntax in either file
  </acceptance_criteria>
  <done>Both utility and login JS files exist with complete ES5-compatible implementations</done>
</task>

<task type="auto">
  <name>Task 3: Create atividades-scan.js and atividades-consulta.js (ES5 scanning workflow and consultation), add COPIED comments to shared.js</name>
  <files>
    cmd/server/templates/atividades/atividades-scan.js
    cmd/server/templates/atividades/atividades-consulta.js
    cmd/server/templates/shared.js
  </files>
  <read_first>
    @cmd/server/templates/app.js — full source to port from (938 lines)
    @cmd/server/templates/shared.js — functions to mark with COPIED comments
    .planning/phases/08-es5-compatibility/08-PATTERNS.md — all §Core pattern sections for scan.js and consulta.js
    .planning/phases/08-es5-compatibility/08-RESEARCH.md — §ES5 Rewrite Strategy, §Share of Work, §ES5 Syntax Constraint Reference, §XHR Wrapper Design
  </read_first>
  <action>
    Create `atividades-scan.js` (~300 lines, extracted from app.js lines 1-791):
    Structure: single `document.addEventListener("DOMContentLoaded", function() { ... })` block containing all code. All `var` declarations at top.

    Within DOMContentLoaded, port these app.js functions to ES5:
    - State management (app.js lines 2-30): Copy `var state = { ... }` object verbatim (already ES5). Copy `saveState()` and `resetActivityState()` verbatim.
    - UI helpers (app.js lines 33-168): Port to ES5:
      * `focusScannerInput(input)` — copy verbatim (already ES5)
      * `refocusInput()` — change `const inputId` → `var inputId`
      * `syncKeyboardUI()` — replace `for (const s of screens)` → `for (var i = 0; i < screens.length; i++)` with `var s = screens[i]`. Replace `.includes()` → `indexOf !== -1` pattern. All template literals → string concatenation.
      * `renderHistory()` — replace `[...arr].reverse()` → `arr.slice(0).reverse()`. Replace `for (const p of reversed)` → index-based for loop. Replace `.find()` → index-based loop. Template literals → string concatenation. Must wrap all innerHTML content in `sanitizeHtml()` per security rules.
      * `showProductDetailModal(seqproduto)` — replace `.find()` → index-based loop. Replace arrow function `() => {}` → `function() {}`. Replace `confirm()` usage verbatim (ES5-compatible).
      * `closeProductDetailModal()` — replace `?.classList.add(...)` → `var el = document.getElementById(...); if (el) el.classList.add("hidden")`
    - Navigation (app.js lines 170-236):
      * `showScreen(screenId)` — replace `.includes()` → index-based loop OR individual `||` checks (per PATTERNS.md §Core pattern — Screen navigation). Replace template literals → string concatenation. Replace `?.` → `&&` guard. Replace `_loadEmpresasCb?.()` → `if (_loadEmpresasCb) { _loadEmpresasCb(); }`
      * `confirmExit(callback)` — replace `.includes()` → indexOf check. Replace arrow function → `function(){}`.
    - Auth (app.js lines 239-263):
      * `logout()` — replace `fetch("/api/auth/logout", ...)` → fire-and-forget XHR: create XMLHttpRequest, open POST to /api/auth/logout, send() (no callback needed). Or simpler: fire-and-forget via `apiPost("/api/auth/logout", null, function(){})`.
    - API loading functions (app.js lines 266-312):
      * `loadEmpresas()` — convert from async/await to XHR callback pattern: `apiGet("/api/empresas", function(ok, status, data) {...}, function() { window.location.href = "/atividades/login"; })` — success handler populates select via sanitizeHtml(), loads first empresa's locais. Replace arrow functions → `function(){}`.
      * `loadLocais(empresaId)` — same async→XHR conversion.
      * `fetchLastActivityInfo()` — same async→XHR conversion. Replace template literals → string concatenation with sanitizeHtml(). Replace `res.data?.dataFim` → `res.data && res.data.dataFim`.
    - Scan functions (app.js lines 315-418):
      * `startActivity()` — async/await → XHR callback pattern. Replace template literals → string concatenation with encodeURIComponent(). All `.includes()` → indexOf.
      * `finalizeActivity()` — async/await → XHR callback pattern. Replace template literals → string concatenation. Replace arrow in `itemHtml = (p) => ...` → inline function. All innerHTML assignments wrapped in sanitizeHtml().
    - Building switch handlers (app.js lines 693-758):
      * Replace arrow `addEventListener("click", async () => {...})` → `addEventListener("click", function() {...})`
      * Replace `async/await` + `fetch` → XHR callback pattern inside the click handler
      * Replace `.includes()` → indexOf, `.some()` → indexOf
      * Replace `new Set(state.expectedProducts.map(...))` → manual dedup with indexOf check
      * Replace spread `push(...newProducts)` → loop push
    - DOMContentLoaded event listeners (app.js lines 432-937): All event handlers and initialization code from app.js DOMContentLoaded block. Replace all arrow functions → `function(){}`. Replace all `?.` → `&&` guards. Replace template literals → string concatenation. Replace `for (let i = 0; i < toggles.length; i++)` → `for (var i = 0; i < toggles.length; i++)`. Replace `async (e) => {}` in form submit handlers → XHR callback patterns.
    - ALL XHR calls must pass onUnauthorized callback that does `window.location.href = "/atividades/login"` (replaces old `showReauthModal(true)` pattern — per D-01, reauth modal is removed, session expiry redirects to full login page)
    - ALL innerHTML assignments must wrap content in `sanitizeHtml()` per security threat model
    - Add global references: `window.showProductDetailModal = showProductDetailModal; window.closeProductDetailModal = closeProductDetailModal;` (app.js lines 433-434)

    Create `atividades-consulta.js` (~150 lines, extracted from app.js lines 793-936):
    Also inside `document.addEventListener("DOMContentLoaded", ...)`:
    - `var consultaMode = "codigo"`
    - `function setConsultaMode(mode)` — copy from app.js lines 795-822, replace template literals → string concatenation, replace `?.` → `&&` guards, no arrow functions
    - Bind consulta mode toggle buttons: `document.getElementById("btn-consulta-mode-codigo").addEventListener("click", function() { setConsultaMode("codigo"); })` — no arrow functions
    - Consulta back button handler — port from app.js lines 826-829
    - Form submit handler for `form-consulta` (app.js lines 830-936): convert async/await → XHR callback pattern. Replace template literals → string concatenation. All innerHTML assignments wrapped in sanitizeHtml(). onUnauthorized → redirect to /atividades/login.
    - The `openConsulta` function (app.js lines 763-790): port to ES5, replace template literals, replace `?.` → `&&` guards

    Update `shared.js`:
    Add a comment line `// COPIED to templates/atividades/atividades-utils.js — TODO: remove when admin/dashboard migrate to ES5 (ES5-05)` ABOVE each of these functions:
    - `async function apiCall(...)` (line 2)
    - `function showLoader(show)` (line 42)
    - `function formatDate(dateStr)` (line 48)
    - `function playBeep(type)` (line 60) 
    - `function escHtml(unsafe)` (line 87)
    - `function sanitizeHtml(dirty)` (line 92)
    Read shared.js lines 1-94 first, insert the comment line directly above each function declaration without changing any other content.
  </action>
  <verify>
    <automated>ls cmd/server/templates/atividades/atividades-scan.js cmd/server/templates/atividades/atividades-consulta.js && grep -q 'function saveState' cmd/server/templates/atividades/atividades-scan.js && grep -q 'function showScreen' cmd/server/templates/atividades/atividades-scan.js && grep -q 'function startActivity' cmd/server/templates/atividades/atividades-scan.js && grep -q 'function finalizeActivity' cmd/server/templates/atividades/atividades-scan.js && grep -q 'function setConsultaMode' cmd/server/templates/atividades/atividades-consulta.js && grep -q 'form-consulta' cmd/server/templates/atividades/atividades-consulta.js && grep -c 'COPIED to templates/atividades/atividades-utils.js' cmd/server/templates/shared.js | xargs test 6 -eq</automated>
  </verify>
  <acceptance_criteria>
    - atividades-scan.js contains state management, UI helpers, navigation, auth, API loading, scan flow, building switch, and finalize functions — all XHR-based with no async/await
    - atividades-consulta.js contains mode toggle, code search, description search, and results rendering — all XHR-based
    - Both files are ES5: zero uses of `const`, `let`, `=>`, `` ` ``, `async`, `fetch`, `?.`
    - Every scannaed product is described via index-based `for` loops, never `.includes()`, `.find()`, `.forEach()`, `.some()`
    - All innerHTML assignments use sanitizeHtml()
    - shared.js has exactly 6 "COPIED" comments — one per ported function
  </acceptance_criteria>
  <done>Scanning workflow and consultation JS created as ES5, shared.js marked with copy traceability</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser JS → Go API | XHR calls from ES5 JS to Go JSON endpoints |
| innerHTML assignment | User-supplied product names/codes rendered into DOM |
| CSRF token extraction | Cookie-based token read via JS and sent as X-CSRF-Token header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering (XSS) | All innerHTML assignments in scan.js, consulta.js | mitigate | Every innerHTML assignment wraps user content in `sanitizeHtml()` (which calls `escHtml()`). Use `innerText` when no HTML formatting needed. |
| T-08-02 | Tampering (CSRF) | XHR apiCall function in atividades-utils.js | mitigate | XHR wrapper reads `csrf_token` cookie and sets X-CSRF-Token header on all POST/PATCH/DELETE requests. Non-mutating GET requests skip CSRF. |
| T-08-03 | Information Disclosure | XHR responses with user/session data | mitigate | Session token is HttpOnly (not accessible from JS). CSRF token is SameSite=Lax. XHR uses `withCredentials=true` for cookie-based auth. |
| T-08-04 | Repudiation | Missing error handling in XHR callbacks | mitigate | Every API call has onSuccess and onUnauthorized callbacks. showLoader(false) called in both paths. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced in this phase. All JS is hand-written ES5. No package manager installs. |
</threat_model>

<verification>
- [ ] All 6 new files exist: atividades-login.html, atividades.html, atividades-utils.js, atividades-login.js, atividades-scan.js, atividades-consulta.js
- [ ] Each HTML template has correct {{define}} block names
- [ ] Each JS file has zero ES6+ syntax features (const, let, =>, `, async, fetch, ?., for...of)
- [ ] Every innerHTML assignment uses sanitizeHtml() wrapper
- [ ] Every mutating XHR call passes CSRF token header
- [ ] onUnauthorized callback in all XHR calls redirects to /atividades/login
- [ ] shared.js has 6 COPIED comments marking ported functions
</verification>

<success_criteria>
Plan 01 is complete when:
1. `cmd/server/templates/atividades/` directory exists with 6 files
2. `atividades-login.html` renders a standalone login page with correct template name
3. `atividades.html` renders the scanning SPA with 5 screens, product modal, and new script references
4. All 4 JS files are pure ES5 with zero modern JS syntax, validated by grep
5. All innerHTML assignments in JS files use sanitizeHtml() as demonstrated by grep for "sanitizeHtml"
6. XHR wrapper includes CSRF token extraction as demonstrated by grep for "csrf_token"
7. shared.js has 6 "COPIED" comment markers
</success_criteria>

<artifacts_produced>
## Artifacts This Phase Produces

### New HTML Templates (in `cmd/server/templates/atividades/`)
- `atividades-login.html` — Go template `atividades-login` for `/atividades/login` route
- `atividades.html` — Go template `atividades` for `/atividades` route (replaces existing template at parse time)

### New ES5 JavaScript Files (in `cmd/server/templates/atividades/`)
- `atividades-utils.js` — Top-level functions: `apiCall`, `apiGet`, `apiPost`, `showLoader`, `formatDate`, `playBeep`, `escHtml`, `sanitizeHtml`
- `atividades-login.js` — DOMContentLoaded handler with session check and login form submission
- `atividades-scan.js` — DOMContentLoaded handler containing: `state` object, `saveState`, `resetActivityState`, `focusScannerInput`, `refocusInput`, `syncKeyboardUI`, `renderHistory`, `showProductDetailModal`, `closeProductDetailModal`, `showScreen`, `confirmExit`, `logout`, `loadEmpresas`, `loadLocais`, `fetchLastActivityInfo`, `startActivity`, `finalizeActivity`, global `window.showProductDetailModal`, `window.closeProductDetailModal`
- `atividades-consulta.js` — DOMContentLoaded handler containing: `consultaMode` variable, `setConsultaMode` function, `openConsulta` logic

### Modified File
- `shared.js` — 6 "COPIED to templates/atividades/atividades-utils.js — TODO" comment lines added (no functional changes)
</artifacts_produced>
