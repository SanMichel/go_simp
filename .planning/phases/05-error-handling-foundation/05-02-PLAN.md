---
phase: 05-error-handling-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - cmd/server/handlers.go
  - cmd/server/auth.go
autonomous: true
requirements:
  - ERR-03
  - ERR-05
must_haves:
  truths:
    - "All handlers.go error paths use a.handleError() instead of inline writeJSON/http.Error/http.NotFound"
    - "All auth.go error paths in requireAPIRole and csrfMiddleware use a.handleError()"
    - "log.Printf('error:...') in handlers.go/auth.go is replaced with slog.ErrorContext"
    - "log.Printf('warn:...') in handlers.go is replaced with slog.WarnContext"
    - "requireRole redirects remain unchanged (they are navigation, not errors)"
    - "loginPost form re-renders remain unchanged (UX feedback, not error responses)"
  artifacts:
    - path: cmd/server/handlers.go
      provides: All handler error paths migrated to a.handleError with AppError
      contains: "a.handleError(w, r"
    - path: cmd/server/auth.go
      provides: requireAPIRole and csrfMiddleware error paths migrated to a.handleError
      contains: "a.handleError"
  key_links:
    - from: cmd/server/handlers.go
      to: cmd/server/errors.go
      via: every error path calls a.handleError which is defined in errors.go
    - from: cmd/server/auth.go
      to: cmd/server/errors.go
      via: requireAPIRole/csrfMiddleware.csrfMiddleware error paths call a.handleError
---

<objective>
Migrate all inline error formatting in handlers.go and auth.go to use the centralized `a.handleError()` dispatcher with `AppError` types.

Purpose: Every error response in the core handler files flows through the standardized pipeline — logged with structured fields, dispatched to correct format (HTML partial for HTMX, JSON for API, text for pages), and consistent user-facing messages.

Output: handlers.go and auth.go with all error paths converted from inline `writeJSON(w, code, map[string]string{"error":...})`, `http.Error(w, ...)`, `http.NotFound(w,r)`, and `log.Printf("error:...")` to `a.handleError(w, r, &AppError{...})` and `slog.ErrorContext`/`slog.WarnContext`.
</objective>

<execution_context>
@/home/bblosangeles/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/bblosangeles/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-error-handling-foundation/05-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@cmd/server/handlers.go
@cmd/server/auth.go
@cmd/server/errors.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all error paths in handlers.go to handleError</name>
  <read_first>
    cmd/server/handlers.go — entire file (535 lines), every `writeJSON`, `http.Error`, `http.NotFound`, `log.Printf("error")`, `log.Printf("warn")`
    cmd/server/errors.go — AppError struct, handleError method, error code constants
    @.planning/phases/05-error-handling-foundation/05-RESEARCH.md — error path inventory table, "After" code example for activityDetails
  </read_first>
  <files>cmd/server/handlers.go</files>
  <action>
    Migrate every error-producing path in handlers.go from inline formatting to `a.handleError(w, r, &AppError{...})`. Use the correct error code constant for each context.

    **Rules for conversion:**

    1. **http.Error(w, msg, code)** → `a.handleError(w, r, &AppError{Code: ERR_CODE, Message: "msg em português", HTTPStatus: code})` where ERR_CODE maps: 400→`ErrCodeBadRequest`, 401→`ErrCodeUnauthorized`, 403→`ErrCodeForbidden`, 404→`ErrCodeNotFound`, 500→`ErrCodeInternal`. Keep the same Portuguese message text.

    2. **http.NotFound(w, r)** (404) → `a.handleError(w, r, &AppError{Code: ErrCodeNotFound, Message: "Não encontrado", HTTPStatus: http.StatusNotFound})`.

    3. **writeJSON(w, code, map[string]string{"error": msg})** → `a.handleError(w, r, &AppError{Code: ERR_CODE, Message: "msg", HTTPStatus: code})`. Per RESEARCH.md Pitfall 2: HTMX endpoints returning HTML errors is correct; the dispatcher checks HX-Request first so HTMX calls to `/api/*` get HTML partials.

    4. **log.Printf("error: %v", err) followed by http.Error** → Keep the log-and-respond pattern but use slog+s.handleError: `slog.ErrorContext(r.Context(), "description", "error", err)` (optional — handleError already logs via slog) + `a.handleError(w, r, &AppError{...})`.

    5. **log.Printf("warn: ...")** with no HTTP response → `slog.WarnContext(r.Context(), "description", "error", err, "id", id)`.

    6. **DO NOT change**: `loginPost` form re-renders with `a.render(w, "login", map[string]string{"Error": msg})` — these are UX feedback, not error responses. `requireRole` redirects — handled separately as they are navigation. `dashboardPage`'s `activities, _ := a.listActivities(...)` — silent error discard pattern stays. `adminUsersSection`'s `users, _ := a.listUsers(...)` — stays.

    **Specific error paths to migrate (in order of appearance in handlers.go):**

    | Line | Current | Code | Message | Replace With |
    |------|---------|------|---------|-------------|
    | 44 | http.Error(500) | ErrCodeInternal | "Erro ao criar sessão" | a.handleError |
    | 86-88 | log.Printf + http.Error(500) | ErrCodeInternal | "Erro ao carregar atividades" | slog.ErrorContext(optional) + a.handleError |
    | 97-98 | http.NotFound | ErrCodeNotFound | "Atividade não encontrada" | a.handleError |
    | 117 | http.Error(400) | ErrCodeBadRequest | "IDs inválidos" | a.handleError |
    | 137 | log.Printf("warn: ...") | — | — | slog.WarnContext |
    | 147-148 | log.Printf + http.Error(500) | ErrCodeInternal | "Erro ao carregar usuários" | a.handleError |
    | 161-162 | log.Printf + http.Error(400) | ErrCodeBadRequest | "Erro ao processar formulário" | a.handleError |
    | 168 | a.render with Error data | ErrCodeValidation | "Dados inválidos." | a.handleError with ErrCodeValidation |
    | 174-176 | http.Error(500) | ErrCodeInternal | "Erro interno do servidor" | a.handleError |
    | 192 | http.NotFound | ErrCodeNotFound | "Usuário não encontrado" | a.handleError |
    | 202 | http.NotFound | ErrCodeNotFound | "Usuário não encontrado" | a.handleError |
    | 212 | http.Error(400) | ErrCodeBadRequest | "Não é possível editar o próprio usuário" | a.handleError |
    | 216-217 | log.Printf + http.Error(400) | ErrCodeBadRequest | "Erro ao processar formulário" | a.handleError |
    | 223 | http.Error(400) | ErrCodeBadRequest | "Role inválido" | a.handleError |
    | 228-229 | http.NotFound | ErrCodeNotFound | "Usuário não encontrado" | a.handleError |
    | 232-233 | http.Error(403) | ErrCodeForbidden | "Sem permissão" | a.handleError |
    | 237-238 | http.Error(500) | ErrCodeInternal | "Erro interno do servidor" | a.handleError |
    | 255 | writeJSON(401) | ErrCodeUnauthorized | "Não autenticado" | a.handleError |
    | 263 | writeJSON(429) | ErrCodeRateLimited | "Muitas tentativas. Aguarde 1 minuto." | a.handleError |
    | 271 | writeJSON(400) | ErrCodeBadRequest | "JSON inválido" | a.handleError |
    | 275 | writeJSON(401) | ErrCodeUnauthorized | "Credenciais inválidas" | a.handleError |
    | 281 | writeJSON(500) | ErrCodeInternal | "Erro ao criar sessão" | a.handleError |
    | 304-305 | writeJSON(500) | ErrCodeInternal | "Erro ao buscar empresas" | a.handleError |
    | 325-326 | writeJSON(500) | ErrCodeInternal | "Erro ao buscar locais" | a.handleError |
    | 346-347 | writeJSON(404) | ErrCodeNotFound | "Produto não encontrado" | a.handleError |
    | 360-361 | writeJSON(404) | ErrCodeNotFound | "Produto não encontrado" | a.handleError |
    | 373-375 | writeJSON(400) | ErrCodeBadRequest | "Parâmetro 'q' é obrigatório" | a.handleError |
    | 379 | writeJSON(500) | ErrCodeInternal | "Erro na consulta" | a.handleError |
    | 398 | writeJSON(500) | ErrCodeInternal | "Erro ao buscar produtos" | a.handleError |
    | 436-438 | writeJSON(400) | ErrCodeBadRequest | "JSON inválido" | a.handleError |
    | 440-443 | writeJSON(400) | ErrCodeBadRequest | "Campos obrigatórios ausentes" | a.handleError |
    | 450-451 | writeJSON(500) | ErrCodeInternal | "Erro ao iniciar transação" | a.handleError |
    | 458 | writeJSON(500) | ErrCodeInternal | "Erro ao salvar atividade" | a.handleError |
    | 463 | writeJSON(500) | ErrCodeInternal | "Erro ao salvar endereço" | a.handleError |
    | 525-526 | log.Printf + writeJSON(500) | ErrCodeInternal | "Erro ao salvar produtos" | slog.ErrorContext + a.handleError |
    | 530-531 | writeJSON(500) | ErrCodeInternal | "Erro ao finalizar atividade" | a.handleError |

    **Imports:** Add `"errors"` if not already imported (needed by none directly but AppError wrapping may use it). Keep all existing imports.
  </action>
  <verify>
    <automated>go build ./cmd/server 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./cmd/server` succeeds
    - `grep -c 'a\.handleError' cmd/server/handlers.go` returns >= 30 (all migrated paths)
    - `grep -c 'writeJSON(w.*map\[string\]string' cmd/server/handlers.go` returns <= 5 (remaining non-error writeJSON calls for success responses like healthCheck, apiEmpresas, apiLocais, apiProduto*, etc.)
    - `grep -c 'http\.Error(w' cmd/server/handlers.go` returns 0 (all migrated)
    - `grep -c 'http\.NotFound(w' cmd/server/handlers.go` returns 0 (all migrated)
    - `grep -c 'log\.Printf' cmd/server/handlers.go` returns <= 2 (remaining warn-level logs or non-error logging only)
    - `grep -c 'ErrCode' cmd/server/handlers.go` returns >= 30
  </acceptance_criteria>
  <done>All 35 error paths in handlers.go converted to a.handleError with appropriate AppError codes and messages. log.Printf("error:...") replaced with slog. log.Printf("warn:...") replaced with slog.WarnContext. Success writeJSON calls kept intact.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate auth.go error paths to handleError</name>
  <read_first>
    cmd/server/auth.go — error paths in requireAPIRole (lines 122-126) and csrfMiddleware (lines 163-176)
    cmd/server/handlers.go — post-migration state (see Task 1)
    @.planning/phases/05-error-handling-foundation/05-RESEARCH.md — open questions: requireRole redirects stay as-is, csrfMiddleware error paths use handleError
  </read_first>
  <files>cmd/server/auth.go</files>
  <action>
    Migrate error-producing paths in auth.go. **DO NOT change** `requireRole` redirects (lines 96-104) — they are navigation, not errors.

    **Paths to migrate:**

    1. **requireAPIRole** (line 122): `writeJSON(w, http.StatusUnauthorized, map[string]string{"error": "Não autorizado"})` → `a.handleError(w, r, &AppError{Code: ErrCodeUnauthorized, Message: "Não autorizado", HTTPStatus: http.StatusUnauthorized})`.

    2. **requireAPIRole** (lines 125-126): `writeJSON(w, http.StatusForbidden, map[string]string{"error": "Sem permissão"})` → `a.handleError(w, r, &AppError{Code: ErrCodeForbidden, Message: "Sem permissão", HTTPStatus: http.StatusForbidden})`.

    3. **csrfMiddleware** (line 163-165): `http.Error(w, "403 Forbidden", http.StatusForbidden)` → `a.handleError(w, r, &AppError{Code: ErrCodeForbidden, Message: "Acesso negado", HTTPStatus: http.StatusForbidden})`.

    4. **csrfMiddleware** (lines 170-171): `writeJSON(w, http.StatusForbidden, map[string]string{"error": "CSRF token ausente"})` → `a.handleError(w, r, &AppError{Code: ErrCodeForbidden, Message: "CSRF token ausente", HTTPStatus: http.StatusForbidden})`.

    5. **csrfMiddleware** (lines 175-176): `writeJSON(w, http.StatusForbidden, map[string]string{"error": "CSRF token inválido"})` → `a.handleError(w, r, &AppError{Code: ErrCodeForbidden, Message: "CSRF token inválido", HTTPStatus: http.StatusForbidden})`.

    **Note on method receiver:** `requireAPIRole` returns `http.HandlerFunc` via closure. The inner function has access to `a` (the App receiver). `csrfMiddleware` similarly has `a` available. Both already call `a.currentUser(r)` etc., so `a.handleError` is accessible.

    Keep `requireRole` (lines 86-110) completely unchanged — redirects are correct behavior.
  </action>
  <verify>
    <automated>go build ./cmd/server 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./cmd/server` succeeds
    - `grep -c 'a\.handleError' cmd/server/auth.go` returns 5 (all 5 error paths migrated)
    - `grep -c 'writeJSON.*map\[string\]string' cmd/server/auth.go` returns 0 (all writeJSON error responses migrated)
    - `grep -c 'http\.Redirect' cmd/server/auth.go` returns >= 4 (requireRole redirects preserved, logout redirect, redirectByRole)
    - `grep -c 'changing own user\|CSRF' cmd/server/auth.go` returns > 0 (CSRF error messages preserved)
    - `go test ./cmd/server -count=1` passes (existing CSRF tests must still pass)
  </acceptance_criteria>
  <done>All 5 auth.go error paths (2 in requireAPIRole, 3 in csrfMiddleware) migrated to a.handleError with AppError. requireRole redirects unchanged. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | Error responses from /api/* now flow through handleError with consistent JSON shape {"error": msg, "code": code} |
| Client→HTMX | Error responses from HTMX endpoints now render error_toast HTML partial instead of raw text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | All migrated error paths | mitigate | Every inline error replaced with AppError + handleError; messages are user-safe Portuguese; raw/wrapped errors logged via slog, never exposed in response body |
| T-05-04 | Information Disclosure | auth.go error paths | mitigate | requireAPIRole and csrfMiddleware errors use AppError with user-safe messages; original error (if any) logged but not exposed |
| T-05-06 | Spoofing | requireRole redirects | accept | Redirects are navigation, not errors; no AppError needed per open question recommendation in RESEARCH.md |
</threat_model>

<verification>
1. `go build ./cmd/server` succeeds
2. `go test ./cmd/server -count=1` passes (all existing tests)
3. Grep for remaining inline error patterns: `grep -c 'http\.Error(w' cmd/server/handlers.go` == 0, `grep -c 'http\.Error(w' cmd/server/auth.go` == 2 only (csrfMiddleware fallbacks if handleError fails — verify)
4. `grep -c 'a\.handleError' cmd/server/handlers.go` >= 30
5. Every error path from inventory table is accounted for
</verification>

<success_criteria>
- `go build ./cmd/server` and `go test ./cmd/server -count=1` both pass
- All 35+ error paths in handlers.go use a.handleError
- All 5 error paths in auth.go use a.handleError
- requireRole redirects preserved (navigation, not errors)
- loginPost form re-renders preserved (UX feedback)
</success_criteria>

## Artifacts This Phase Produces

| Artifact | Kind | Plan |
|----------|------|------|
| Converted `loginPost` makeToken error (line 44) | Inline→handleError | 05-02 |
| Converted `dashboardTable` error (lines 86-88) | Inline→handleError | 05-02 |
| Converted `activityDetails` 404 (lines 97-98) | Inline→handleError | 05-02 |
| Converted `printBulk` 400 (line 117) | Inline→handleError | 05-02 |
| Converted `printActivities` warn log (line 137) | log.Printf→slog.WarnContext | 05-02 |
| Converted `adminPage` error (lines 147-148) | Inline→handleError | 05-02 |
| Converted `adminCreateUser` errors (lines 161-176) | Inline→handleError | 05-02 |
| Converted `adminEditUserRow` 404 (line 192) | Inline→handleError | 05-02 |
| Converted `adminUserRow` 404 (line 202) | Inline→handleError | 05-02 |
| Converted `adminUpdateUser` errors (lines 212-238) | Inline→handleError | 05-02 |
| Converted `apiMe` 401 (line 255) | Inline→handleError | 05-02 |
| Converted `apiLogin` rate limit/400/401/500 (lines 263-281) | Inline→handleError | 05-02 |
| Converted `apiEmpresas` 500 (lines 304-305) | Inline→handleError | 05-02 |
| Converted `apiLocais` 500 (lines 325-326) | Inline→handleError | 05-02 |
| Converted `apiProdutoEAN` 404 (lines 346-347) | Inline→handleError | 05-02 |
| Converted `apiProdutoConsulta` 404 (lines 360-361) | Inline→handleError | 05-02 |
| Converted `apiProdutoConsultaDescricao` errors (lines 373-379) | Inline→handleError | 05-02 |
| Converted `apiProdutosLocal` 500 (line 398) | Inline→handleError | 05-02 |
| Converted `apiFinalizar` errors (lines 436-531) | Inline→handleError | 05-02 |
| Converted `requireAPIRole` unauthorized (line 122) | Inline→handleError | 05-02 |
| Converted `requireAPIRole` forbidden (lines 125-126) | Inline→handleError | 05-02 |
| Converted `csrfMiddleware` origin mismatch (lines 163-165) | Inline→handleError | 05-02 |
| Converted `csrfMiddleware` token missing (lines 170-171) | Inline→handleError | 05-02 |
| Converted `csrfMiddleware` token invalid (lines 175-176) | Inline→handleError | 05-02 |

<output>
Create `.planning/phases/05-error-handling-foundation/05-02-SUMMARY.md` when done
</output>
