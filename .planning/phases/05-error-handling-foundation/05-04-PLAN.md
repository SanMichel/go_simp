---
phase: 05-error-handling-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - cmd/server/handlers.go
  - cmd/server/activity_handlers.go
  - cmd/server/dashboard_handlers.go
  - cmd/server/admin_handlers.go
  - cmd/server/templates/shared.js
autonomous: true
requirements:
  - HAND-06
  - ES5-02
must_haves:
  truths:
    - "handlers.go is trimmed to ~150 lines containing only page/entrypoint handlers (home, loginPage, loginPost, logout, healthCheck, atividadesPage, apiMe, apiLogin, apiLogout)"
    - "activity_handlers.go contains scanning/activity handlers (apiEmpresas, apiLocais, apiProdutoEAN, apiProdutoConsulta, apiProdutoConsultaDescricao, apiProdutosLocal, apiFinalizar, apiLastInfo)"
    - "dashboard_handlers.go contains dashboard handlers (dashboardPage, dashboardTable, activityDetails, printOne, printBulk, printActivities)"
    - "admin_handlers.go contains admin handlers (adminPage, adminUsersSection, adminCreateUser, adminEditUserRow, adminUserRow, adminUpdateUser)"
    - "DOMPurify code is removed from shared.js; sanitizeHtml delegates to escHtml()"
  artifacts:
    - path: cmd/server/activity_handlers.go
      provides: Scanning/activity HTTP handlers
      contains: "func.*apiEmpresas\|func.*apiFinalizar"
    - path: cmd/server/dashboard_handlers.go
      provides: Dashboard page/partial handlers
      contains: "func.*dashboardPage\|func.*dashboardTable"
    - path: cmd/server/admin_handlers.go
      provides: Admin CRUD handlers
      contains: "func.*adminPage\|func.*adminCreateUser"
    - path: cmd/server/handlers.go
      provides: Trimmed to generic/page handlers only
      contains: "func.*home\|func.*loginPage"
    - path: cmd/server/templates/shared.js
      provides: ES5-02 fix — DOMPurify removed, escHtml used
      contains: "function sanitizeHtml"
  key_links:
    - from: cmd/server/handlers.go
      to: cmd/server/activity_handlers.go
      via: apiEmpresas etc moved from handlers.go to activity_handlers.go
    - from: cmd/server/handlers.go
      to: cmd/server/dashboard_handlers.go
      via: dashboardPage etc moved from handlers.go to dashboard_handlers.go
    - from: cmd/server/handlers.go
      to: cmd/server/admin_handlers.go
      via: adminPage etc moved from handlers.go to admin_handlers.go
---

<objective>
Reorganize monolithic handlers.go into domain-grouped files and replace DOMPurify with escHtml in shared.js.

Purpose: Reduce cognitive load by organizing ~535 lines of handlers into focused files. Each file corresponds to a UI domain: activity scanning, dashboard, admin. The generic handlers stay in handlers.go. This is a mechanical split — no behavior changes. Additionally, remove ~1060 lines of vendored DOMPurify library code and replace with existing escHtml() function.

Output: Four handler files (trimmed handlers.go + 3 new domain files), updated shared.js.
</objective>

<execution_context>
@/home/bblosangeles/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/bblosangeles/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-error-handling-foundation/05-RESEARCH.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@cmd/server/handlers.go
@cmd/server/api_handlers.go
@cmd/server/templates/shared.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split handlers.go into domain files</name>
  <read_first>
    cmd/server/handlers.go — full file (535 lines), post-migration from Plan 05-02. Every function already uses a.handleError.
    cmd/server/api_handlers.go — existing structure to replicate (same package main, same import style)
    @.planning/phases/05-error-handling-foundation/05-RESEARCH.md — File Organization Plan section with split map
  </read_first>
  <files>cmd/server/activity_handlers.go, cmd/server/dashboard_handlers.go, cmd/server/admin_handlers.go, cmd/server/handlers.go</files>
  <action>
    This is a mechanical file split. All error paths are already migrated (Plan 05-02). Create three new files and trim handlers.go.

    **1. Create `cmd/server/activity_handlers.go`:**

    Package `main` with imports: `"context"`, `"database/sql"`, `"encoding/json"`, `"log/slog"`, `"net/http"`, `"strconv"`, `"strings"`, `"time"`.

    Move these functions (exact copies from handlers.go, post-migration state with a.handleError calls):

    - `func (a *App) apiEmpresas(w http.ResponseWriter, r *http.Request, u *User)` — Oracle empresa query, error via a.handleError
    - `func (a *App) apiLocais(w http.ResponseWriter, r *http.Request, u *User)` — Oracle local query, error via a.handleError
    - `func (a *App) apiProdutoEAN(w http.ResponseWriter, r *http.Request, u *User)` — Oracle product by EAN, error via a.handleError
    - `func (a *App) apiProdutoConsulta(w http.ResponseWriter, r *http.Request, u *User)` — Oracle full product lookup, error via a.handleError
    - `func (a *App) apiProdutoConsultaDescricao(w http.ResponseWriter, r *http.Request, u *User)` — Oracle product by description, error via a.handleError
    - `func (a *App) apiProdutosLocal(w http.ResponseWriter, r *http.Request, u *User)` — Oracle products by location, error via a.handleError
    - `func (a *App) apiLastInfo(w http.ResponseWriter, r *http.Request, u *User)` — Last activity info query, error via a.handleError (currently returns writeJSON 200 on error — keep as-is: `a.handleError` with ErrCodeNotFound)
    - `func (a *App) apiFinalizar(w http.ResponseWriter, r *http.Request, u *User)` — Transaction-heavy activity finalization, all errors via a.handleError

    Total: ~180 lines.

    **2. Create `cmd/server/dashboard_handlers.go`:**

    Package `main` with imports: `"log/slog"`, `"net/http"`, `"strconv"`, `"strings"`.

    Move these functions (exact copies from handlers.go):

    - `func (a *App) dashboardPage(w http.ResponseWriter, r *http.Request)` — Dashboard page render
    - `func (a *App) dashboardTable(w http.ResponseWriter, r *http.Request)` — Dashboard table HTMX partial, error via a.handleError
    - `func (a *App) activityDetails(w http.ResponseWriter, r *http.Request)` — Activity modal partial, error via a.handleError
    - `func (a *App) printOne(w http.ResponseWriter, r *http.Request)` — Single activity print view
    - `func (a *App) printBulk(w http.ResponseWriter, r *http.Request)` — Bulk activity print, error via a.handleError
    - `func (a *App) printActivities(w http.ResponseWriter, r *http.Request, ids []int)` — Print rendering, slog.WarnContext for errors

    Total: ~80 lines.

    **3. Create `cmd/server/admin_handlers.go`:**

    Package `main` with imports: `"log/slog"`, `"net/http"`, `"strconv"`.

    Move these functions (exact copies from handlers.go):

    - `func (a *App) adminPage(w http.ResponseWriter, r *http.Request)` — Admin page render, error via a.handleError
    - `func (a *App) adminUsersSection(w http.ResponseWriter, r *http.Request)` — Users section HTMX partial
    - `func (a *App) adminCreateUser(w http.ResponseWriter, r *http.Request)` — User creation handler, errors via a.handleError
    - `func (a *App) adminEditUserRow(w http.ResponseWriter, r *http.Request)` — User edit row partial, error via a.handleError
    - `func (a *App) adminUserRow(w http.ResponseWriter, r *http.Request)` — User row partial, error via a.handleError
    - `func (a *App) adminUpdateUser(w http.ResponseWriter, r *http.Request)` — User update handler, errors via a.handleError

    Total: ~100 lines.

    **4. Trim `cmd/server/handlers.go`:**

    Remove all functions that were moved. Keep only:

    - `home`, `loginPage`, `loginPost`, `healthCheck`, `logout`, `atividadesPage`
    - `apiMe`, `apiLogin`, `apiLogout`
    - Imports: Remove imports no longer needed after moving functions (e.g. `"context"`, `"database/sql"`, `"encoding/json"`, `"strconv"`, `"strings"` if only used by moved functions). Keep `"net/http"`, `"log/slog"`, `"golang.org/x/crypto/bcrypt"`.

    Total remaining: ~150 lines.

    **Critical rules:**
    - Copy functions verbatim — do NOT change logic, error handling, or any behavior.
    - Each new file MUST have `package main` and its own `import` block.
    - Each new file exports no new symbols — all functions are lowercase (package-internal access).
    - Remove function declarations from handlers.go after copying to new files.
    - Adjust imports in handlers.go — keep only those needed by remaining functions.
  </action>
  <verify>
    <automated>go build ./cmd/server 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./cmd/server` succeeds
    - `go test ./cmd/server -count=1` passes (no behavior change)
    - `grep -c 'func.*that was moved' cmd/server/handlers.go` returns 0 for all moved function names
    - `ls -la cmd/server/activity_handlers.go` exists
    - `ls -la cmd/server/dashboard_handlers.go` exists
    - `ls -la cmd/server/admin_handlers.go` exists
    - `grep -c 'func.*apiFinalizar' cmd/server/activity_handlers.go` returns 1
    - `grep -c 'func.*dashboardPage' cmd/server/dashboard_handlers.go` returns 1
    - `grep -c 'func.*adminCreateUser' cmd/server/admin_handlers.go` returns 1
    - `grep -c 'func.*loginPost\|func.*home\|func.*apiMe' cmd/server/handlers.go` returns >= 1 for each
    - `wc -l < cmd/server/handlers.go` <= 200 (trimmed from 535)
  </acceptance_criteria>
  <done>handlers.go trimmed to ~150 lines (page/entrypoint handlers only). activity_handlers.go, dashboard_handlers.go, admin_handlers.go created with domain-grouped handlers. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: ES5-02 — Replace DOMPurify with escHtml in shared.js</name>
  <read_first>
    cmd/server/templates/shared.js — find the DOMPurify library code block and sanitizeHtml function
    @.planning/phases/05-error-handling-foundation/05-RESEARCH.md — ES5-02 section (lines 563-579)
  </read_first>
  <files>cmd/server/templates/shared.js</files>
  <action>
    Per RESEARCH.md ES5-02 section and A3 assumption log:

    1. **Remove DOMPurify library code:** Locate and delete lines 42 through 1110 (inclusive). This starts at `// node_modules/dompurify/dist/purify.es.mjs` (line 42) and ends at `var purify = createDOMPurify();` plus the blank line before `escHtml`. Exact block: from line 42 `// node_modules/dompurify/dist/purify.es.mjs` through the blank line ~1110 before the `escHtml` function. Confirm by reading after the purgeify var assignment for trailing blank lines. The block is approximately 1060 lines.

    2. **Fix sanitizeHtml function** (currently around line 1157-1159): Change from `return purify.sanitize(dirty);` to `return escHtml(dirty);`.

    Per assumption A3: `escHtml` escapes `&<>"'` characters. All existing `sanitizeHtml()` call sites pass trusted template strings with interpolated product descriptions — `escHtml` on the interpolated values is a strict superset of safety.

    **No other changes to shared.js.** The `escHtml` function (currently lines 1152-1156) and all other code remain untouched.
  </action>
  <verify>
    <automated>ls -la cmd/server/templates/shared.js && go build ./cmd/server 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'purify\|DOMPurify\|createDOMPurify' cmd/server/templates/shared.js` returns 0 (all DOMPurify code removed)
    - `grep -c 'sanitizeHtml' cmd/server/templates/shared.js` returns 1 (the function wrapper)
    - `grep 'sanitizeHtml' cmd/server/templates/shared.js` shows `escHtml(dirty)` — not `purify.sanitize`
    - `grep -c 'function escHtml' cmd/server/templates/shared.js` returns 1 (preserved)
    - `go build ./cmd/server` succeeds (shared.js is embedded but not compiled by Go; this confirms embed doesn't break)
    - File size reduced by ~1050-1060 lines: `wc -l < cmd/server/templates/shared.js` <= 110 (was 1159, removing ~1060 lines leaves ~100 lines)
  </acceptance_criteria>
  <done>DOMPurify library code (~1060 lines) removed from shared.js. sanitizeHtml delegates to escHtml(). escHtml function preserved. Build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File split | No new trust boundaries — same code, same package, split into domain files |
| JS sanitization | Client-side sanitization via escHtml replaces DOMPurify |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | File split (HAND-06) | accept | Mechanical copy-paste split of handler functions between files in same package. Zero behavior change. Build succeeds = no dangling references. All tests pass = no regression. |
| T-05-09 | Information Disclosure | escHtml replacement (ES5-02) | accept | escHtml escapes `&<>"'` characters. Per RESEARCH.md A3: all existing sanitizeHtml() call sites pass trusted template strings with interpolated product descriptions. escHtml on interpolated values is strict superset of DOMPurify behavior for this use case. |
</threat_model>

<verification>
1. `go build ./cmd/server` succeeds (no compilation errors from file split)
2. `go test ./cmd/server -count=1` passes (no behavior changes)
3. Manual check: `grep -c 'DOMPurify\|purify' cmd/server/templates/shared.js` == 0
4. `grep 'sanitizeHtml' cmd/server/templates/shared.js` shows `escHtml(dirty)` call
5. Server starts: `go run ./cmd/server 2>&1 | head -3` — no startup errors
</verification>

<success_criteria>
- `go build ./cmd/server` and `go test ./cmd/server -count=1` both pass
- handlers.go trimmed from 535 to ~150 lines
- activity_handlers.go (~180 lines), dashboard_handlers.go (~80 lines), admin_handlers.go (~100 lines) exist
- shared.js DOMPurify block removed (~1060 lines removed)
- sanitizeHtml uses `escHtml(dirty)` instead of `purify.sanitize(dirty)`
</success_criteria>

## Artifacts This Phase Produces

| Artifact | Kind | Plan |
|----------|------|------|
| `activity_handlers.go` file | File | 05-04 |
| `dashboard_handlers.go` file | File | 05-04 |
| `admin_handlers.go` file | File | 05-04 |
| Trimmed `handlers.go` (~150 lines) | File edit | 05-04 |
| `shared.js` sanitizeHtml → escHtml | File edit | 05-04 |
| `shared.js` DOMPurify block removed | File edit | 05-04 |

<output>
Create `.planning/phases/05-error-handling-foundation/05-04-SUMMARY.md` when done
</output>
